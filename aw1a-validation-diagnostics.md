# AW-1A Validation Diagnostics

Run ID: 29725533036

| Check | Outcome |
|---|---|
| npm ci | success |
| typecheck | success |
| lint | success |
| targeted tests | success |
| full unit tests | failure |
| starting-main failure parity | success |
| script tests | failure |
| npm audit | success |
| build | success |
| raw server QA | success |
| Playwright install | success |
| rendered browser QA | failure |
| scope boundaries | success |

## browser-qa.log — final output
```text
node:internal/modules/run_main:107
    triggerUncaughtException(
    ^

locator.waitFor: Timeout 20000ms exceeded.
Call log:
  - waiting for locator('select:has(option[value="de"])') to be visible

    at /home/runner/work/gymsands/gymsands/scripts/aw1a-browser-qa.mjs:82:22 {
  log: [
    `  - waiting for locator('select:has(option[value="de"])') to be visible`
  ],
  name: 'TimeoutError'
}

Node.js v24.18.0
```

## build.log — final output
```text
├ ƒ /api/internal/maintenance/oauth-cleanup
├ ƒ /api/internal/maintenance/privacy-lifecycle
├ ƒ /api/mcp
├ ƒ /api/mcp/activity
├ ƒ /api/mcp/connections
├ ƒ /api/oauth/authorize
├ ƒ /api/oauth/register
├ ƒ /api/oauth/revoke
├ ƒ /api/oauth/token
├ ƒ /api/observability/client-error
├ ƒ /api/user/ai-permissions
├ ƒ /api/user/consents
├ ƒ /api/user/data-export
├ ƒ /api/user/privacy-requests
├ ƒ /api/version
├ ƒ /api/workout-plan/replace-exercise
├ ƒ /api/workouts/sessions/[id]/muscle-analysis
├ ○ /apple-icon.png
├ ƒ /auth/consent-completion
├ ƒ /auth/oauth-complete
├ ƒ /calories
├ ƒ /calories/custom-food-meal
├ ƒ /calories/food-hub
├ ƒ /calories/weekly-overview
├ ƒ /daily-fit-tasks
├ ƒ /dashboard
├ ƒ /forgot-password
├ ƒ /habits
├ ƒ /hydration
├ ○ /icon.png
├ ƒ /legal/disclaimer
├ ƒ /legal/impressum
├ ƒ /legal/privacy
├ ƒ /legal/terms
├ ƒ /login
├ ƒ /my-meal-plan
├ ƒ /my-meal-plan/food-preferences
├ ƒ /my-workout
├ ƒ /my-workout/day/[dayId]
├ ƒ /my-workout/day/[dayId]/add-exercise
├ ƒ /my-workout/exercises/[exerciseId]
├ ƒ /my-workout/muscle-load
├ ƒ /my-workout/plans
├ ƒ /my-workout/plans/[planId]
├ ƒ /my-workout/plans/[planId]/edit
├ ƒ /my-workout/plans/builder
├ ƒ /oauth/authorize
├ ƒ /onboarding
├ ƒ /personal-records
├ ƒ /profile
├ ƒ /progress
├ ƒ /register
├ ƒ /reset-password
├ ƒ /settings
├ ƒ /settings/account
├ ƒ /settings/ai-imports
├ ƒ /settings/ai-imports/chatgpt-setup
├ ƒ /settings/coaching-profile
├ ƒ /settings/connections
├ ƒ /settings/connections/chatgpt
├ ƒ /settings/data-privacy
├ ƒ /settings/nutrition-targets
├ ƒ /settings/preferences
├ ƒ /settings/reminders
├ ƒ /settings/subscription
├ ƒ /sleep-recovery
├ ƒ /supplements
├ ƒ /today-workout
├ ƒ /welcome
├ ƒ /wellness
├ ƒ /workout-history
├ ƒ /workouts
├ ƒ /workouts/[id]
├ ƒ /workouts/session/[id]
└ ƒ /workouts/session/day/[dayId]


○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand

```

## development-server.log — final output
```text

> plaivra@1.0.0 dev
> next dev

▲ Next.js 16.2.10 (Turbopack)
- Local:         http://localhost:3100
- Network:       http://10.1.0.194:3100
✓ Ready in 314ms
Attention: Next.js now collects completely anonymous telemetry regarding usage.
This information is used to shape Next.js' roadmap and prioritize features.
You can learn more, including how to opt-out if you'd not like to participate in this anonymous program, by visiting the following URL:
https://nextjs.org/telemetry


○ Compiling / ...
 GET / 200 in 5.4s (next.js: 5.1s, application-code: 344ms)
 GET / 200 in 58ms (next.js: 2ms, application-code: 56ms)
 GET /settings/preferences 200 in 1151ms (next.js: 1017ms, application-code: 135ms)
⚠ Blocked cross-origin request to Next.js dev resource /_next/webpack-hmr from "127.0.0.1".
Cross-origin access to Next.js dev resources is blocked by default for safety.

To allow this host in development, add it to "allowedDevOrigins" in next.config.js and restart the dev server:

// next.config.js
module.exports = {
  allowedDevOrigins: ['127.0.0.1'],
}

Read more: https://nextjs.org/docs/app/api-reference/config/next-config-js/allowedDevOrigins
```

## lint.log — final output
```text

> plaivra@1.0.0 lint
> eslint .

```

## main-unit-tests.log — final output
```text
[31m+         shell: bash[39m
[31m+         run: |[39m
[31m+           set -euo pipefail[39m
[31m+           export PLAIVRA_VALIDATE_PRODUCTION_ENV=true[39m
[31m+           export PLAIVRA_RELEASE_ENVIRONMENT=production[39m
[31m+           export NEXT_PUBLIC_APP_URL=https://app.plaivra.com[39m
[31m+           export NEXT_PUBLIC_USE_MOCK_AUTH=false[39m
[31m+           export PLAIVRA_MCP_BASE_URL=https://app.plaivra.com/api/mcp[39m
[31m+           export PLAIVRA_OAUTH_ISSUER=https://app.plaivra.com[39m
[31m+           export PLAIVRA_CIMD_ALLOWED_ORIGINS=https://chatgpt.com[39m
[31m+           export CRON_SECRET=ci-placeholder-cron-secret-that-is-long-enough[39m
[31m+           npm run validate:env[39m
[31m+[39m
[31m+       - name: Build production application[39m
[31m+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'[39m
[31m+         shell: bash[39m
[31m+         run: |[39m
[31m+           set -euo pipefail[39m
[31m+           export PLAIVRA_BUILD_TIMESTAMP="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"[39m
[31m+           NEXT_PUBLIC_USE_MOCK_AUTH=false npm run build[39m
[31m+[39m
[31m+       - name: Install Playwright Chromium[39m
[31m+         if: steps.scope.outputs.ui == 'true'[39m
[31m+         run: npx playwright install --with-deps chromium[39m
[31m+[39m
[31m+       - name: Run relevant browser QA[39m
[31m+         if: steps.scope.outputs.ui == 'true'[39m
[31m+         shell: bash[39m
[31m+         run: |[39m
[31m+           set -euo pipefail[39m
[31m+           mkdir -p quality-reports[39m
[31m+           NEXT_PUBLIC_USE_MOCK_AUTH=true npm run dev > quality-reports/qa-server.log 2>&1 &[39m
[31m+           server_pid=$![39m
[31m+           trap 'kill "$server_pid" 2>/dev/null || true' EXIT[39m
[31m+           for attempt in $(seq 1 60); do[39m
[31m+             if curl --fail --silent http://localhost:3000/ > /dev/null; then break; fi[39m
[31m+             sleep 1[39m
[31m+           done[39m
[31m+           curl --fail --silent http://localhost:3000/ > /dev/null[39m
[31m+[39m
[31m+           if [ "${{ steps.scope.outputs.train_ui }}" = "true" ]; then[39m
[31m+             QA_BASE_URL=http://localhost:3000 QA_TRAIN_EVIDENCE_DIR=quality-reports/train-qa-evidence npm run qa:train[39m
[31m+           else[39m
[31m+             QA_BASE_URL=http://localhost:3000 QA_EVIDENCE_DIR=quality-reports/rendered-qa-evidence npm run qa:rendered[39m
[31m+           fi[39m
[31m+[39m
[31m+       - name: Upload browser evidence on failure[39m
[31m+         if: failure() && steps.scope.outputs.ui == 'true'[39m
[31m+         uses: actions/upload-artifact@v4[39m
[31m+         with:[39m
[31m+           name: quality-failure-evidence-${{ github.run_id }}[39m
[31m+           path: quality-reports/[39m
[31m+           if-no-files-found: ignore[39m
[31m+           retention-days: 7[39m
[31m+[39m

[36m [2m❯[22m lib/product/train-phase2a-architecture.test.ts:[2m96:22[22m[39m
    [90m 94|[39m     expect(verification).toContain("failed detach left a partial clone…
    [90m 95|[39m     expect(verification).toContain("account deletion did not remove tr…
    [90m 96|[39m     expect(workflow).toContain("-f supabase/verification/train-phase2a…
    [90m   |[39m                      [31m^[39m
    [90m 97|[39m   })[33m;[39m
    [90m 98|[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/4]⎯[22m[39m


[2m Test Files [22m [1m[31m3 failed[39m[22m[2m | [22m[1m[32m174 passed[39m[22m[90m (177)[39m
[2m      Tests [22m [1m[31m4 failed[39m[22m[2m | [22m[1m[32m1135 passed[39m[22m[90m (1139)[39m
[2m   Start at [22m 07:45:34
[2m   Duration [22m 17.50s[2m (transform 4.31s, setup 0ms, import 13.05s, tests 6.09s, environment 2.54s)[22m


::error file=/tmp/aw1a-main/lib/product/muscle-intelligence-phase1-migration.test.ts,title=lib/product/muscle-intelligence-phase1-migration.test.ts > Muscle Intelligence Phase 1 migration contract > executes the disposable Phase 1 verification in the authoritative Quality database preflight,line=101,column=29::AssertionError: expected 'name: Quality\n\non:\n  pull_request:…' to contain 'PGPASSWORD=postgres psql "postgresql:…'%0A%0A- Expected%0A+ Received%0A%0A- PGPASSWORD=postgres psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -X -v ON_ERROR_STOP=1 -f supabase/verification/muscle-intelligence-phase1.sql%0A+ name: Quality%0A+%0A+ on:%0A+   pull_request:%0A+     branches:%0A+       - main%0A+   workflow_dispatch:%0A+%0A+ permissions:%0A+   contents: read%0A+%0A+ concurrency:%0A+   group: quality-${{ github.workflow }}-${{ github.ref }}%0A+   cancel-in-progress: true%0A+%0A+ jobs:%0A+   verify:%0A+     runs-on: ubuntu-latest%0A+     timeout-minutes: 30%0A+     env:%0A+       NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co%0A+       NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-public-key-long-enough%0A+       SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-service-role-key-long-enough%0A+       NEXT_PUBLIC_APP_URL: http://localhost:3000%0A+       NEXT_PUBLIC_USE_MOCK_AUTH: "true"%0A+       PLAIVRA_MCP_BASE_URL: http://localhost:3000/api/mcp%0A+       PLAIVRA_MCP_TOKEN_SECRET: ci-placeholder-token-secret-that-is-long-enough%0A+       PLAIVRA_CHATGPT_REDIRECT_URIS: https://chatgpt.com/connector_platform_oauth_redirect%0A+       PLAIVRA_CIMD_ALLOWED_ORIGINS: https://chatgpt.com%0A+       PLAIVRA_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}%0A+       PLAIVRA_RELEASE_ENVIRONMENT: ci%0A+       PLAIVRA_SCHEMA_COMPATIBILITY_VERSION: "2"%0A+%0A+     steps:%0A+       - name: Checkout exact revision%0A+         uses: actions/checkout@v4%0A+         with:%0A+           ref: ${{ github.event.pull_request.head.sha || github.sha }}%0A+           fetch-depth: 0%0A+%0A+       - name: Verify checkout identity%0A+         shell: bash%0A+         run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha || github.sha }}"%0A+%0A+       - name: Detect relevant verification scope%0A+         id: scope%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             changed_files="$(git ls-files)"%0A+           else%0A+             base_sha="${{ github.event.pull_request.base.sha }}"%0A+             changed_files="$(git diff --name-only "${base_sha}...HEAD")"%0A+           fi%0A+%0A+           printf '%25s\n' "$changed_files"%0A+%0A+           code=false%0A+           runtime=false%0A+           dependencies=false%0A+           database=false%0A+           integration=false%0A+           scripts=false%0A+           environment=false%0A+           ui=false%0A+           train_ui=false%0A+%0A+           if grep -Eq '\.(ts|tsx|js|jsx|mjs|cjs)$|^(package(-lock)?\.json|tsconfig\.json|vitest\..*\.mjs|eslint\.config\.)' <<< "$changed_files"; then%0A+             code=true%0A+           fi%0A+%0A+           if grep -Eq '^(app/|components/|lib/|services/|types/|middleware\.|next\.config\.|package\.json)' <<< "$changed_files" \%0A+             && grep -Eqv '(\.test\.|\.spec\.|^docs/)' <<< "$changed_files"; then%0A+             runtime=true%0A+           fi%0A+%0A+           if grep -Eq '^package-lock\.json$' <<< "$changed_files"; then dependencies=true; fi%0A+           if grep -Eq '^(supabase/migrations/|supabase/migration-ledger\.json|scripts/(check-migration-ledger|test-database-preflight-control)|services/database/|types/database\.ts)' <<< "$changed_files"; then database=true; fi%0A+           if grep -Eq '^(services/database/|app/api/|supabase/migrations/|vitest\.integration\.config\.mjs)' <<< "$changed_files"; then integration=true; fi%0A+           if grep -Eq '^scripts/' <<< "$changed_files"; then scripts=true; fi%0A+           if grep -Eq '^(scripts/validate-production-env\.mjs|lib/integrations/env|next\.config\.|vercel\.json|package-lock\.json)' <<< "$changed_files"; then environment=true; fi%0A+           if grep -Eq '^(app/|components/|public/|styles/|lib/i18n/|next\.config\.|postcss\.config\.|tailwind\.config\.)' <<< "$changed_files"; then ui=true; fi%0A+           if grep -Eq '^(app/.*/train/|components/train/|components/workouts/|lib/train/|services/activity-catalog/|app/api/activity-catalog/)' <<< "$changed_files"; then train_ui=true; fi%0A+%0A+           {%0A+             echo "code=${code}"%0A+             echo "runtime=${runtime}"%0A+             echo "dependencies=${dependencies}"%0A+             echo "database=${database}"%0A+             echo "integration=${integration}"%0A+             echo "scripts=${scripts}"%0A+             echo "environment=${environment}"%0A+             echo "ui=${ui}"%0A+             echo "train_ui=${train_ui}"%0A+           } >> "$GITHUB_OUTPUT"%0A+%0A+       - name: Set up Node.js%0A+         if: steps.scope.outputs.code == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         uses: actions/setup-node@v4%0A+         with:%0A+           node-version: 24%0A+           cache: npm%0A+%0A+       - name: Install dependencies%0A+         if: steps.scope.outputs.code == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         run: npm ci%0A+%0A+       - name: Lint changed source files%0A+         if: steps.scope.outputs.code == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             mapfile -d '' files < <(git ls-files -z | grep -zE '\.(ts|tsx|js|jsx|mjs|cjs)$' || true)%0A+           else%0A+             base_sha="${{ github.event.pull_request.base.sha }}"%0A+             mapfile -d '' files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" | grep -zE '\.(ts|tsx|js|jsx|mjs|cjs)$' || true)%0A+           fi%0A+           if [ "${#files[@]}" -gt 0 ]; then%0A+             npx eslint -- "${files[@]}"%0A+           fi%0A+%0A+       - name: Typecheck application%0A+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         run: npm run typecheck%0A+%0A+       - name: Run tests related to changed code%0A+         if: steps.scope.outputs.code == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             npm run test:unit%0A+             exit 0%0A+           fi%0A+%0A+           base_sha="${{ github.event.pull_request.base.sha }}"%0A+           mapfile -d '' test_files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" | grep -zE '\.(test|spec)\.(ts|tsx|js|jsx)$' || true)%0A+           if [ "${#test_files[@]}" -gt 0 ]; then%0A+             npx vitest run --config vitest.unit.config.mjs "${test_files[@]}"%0A+           fi%0A+%0A+           mapfile -d '' source_files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" \%0A+             | grep -zE '\.(ts|tsx|js|jsx)$' \%0A+             | grep -zEv '\.(test|spec)\.(ts|tsx|js|jsx)$' || true)%0A+           if [ "${#source_files[@]}" -gt 0 ]; then%0A+             npx vitest related "${source_files[@]}" --run --config vitest.unit.config.mjs --passWithNoTests%0A+           fi%0A+%0A+       - name: Check changed production dependencies%0A+         if: steps.scope.outputs.dependencies == 'true'%0A+         run: npm audit --omit=dev --audit-level=moderate%0A+%0A+       - name: Run changed script tests%0A+         if: steps.scope.outputs.scripts == 'true'%0A+         run: npm run test:scripts%0A+%0A+       - name: Run integration tests for API or database changes%0A+         if: steps.scope.outputs.integration == 'true'%0A+         run: npm run test:integration%0A+%0A+       - name: Set up Supabase CLI%0A+         if: steps.scope.outputs.database == 'true'%0A+         uses: supabase/setup-cli@v2%0A+         with:%0A+           version: 2.109.1%0A+           github-token: ${{ github.token }}%0A+%0A+       - name: Replay and lint database migrations%0A+         if: steps.scope.outputs.database == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           supabase db start%0A+           supabase db reset --local --no-seed%0A+           supabase db lint --local --schema public --level error --fail-on error%0A+           npm run migration:ledger:check%0A+%0A+       - name: Verify production database security contracts%0A+         if: steps.scope.outputs.database == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           database_url="postgresql://postgres:postgres@127.0.0.1:54322/postgres"%0A+           PGPASSWORD=postgres psql "$database_url" -X -v ON_ERROR_STOP=1 -f supabase/verification/train-atomic-rpc-security.sql%0A+           PLAIVRA_PREFLIGHT_TEST_DATABASE_URL="$database_url" node scripts/test-database-preflight-control.mjs%0A+           PGPASSWORD=postgres psql "$database_url" -X -v ON_ERROR_STOP=1 -f supabase/verification/production-release-migration-preflight.sql%0A+%0A+       - name: Validate production environment contract%0A+         if: steps.scope.outputs.environment == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           export PLAIVRA_VALIDATE_PRODUCTION_ENV=true%0A+           export PLAIVRA_RELEASE_ENVIRONMENT=production%0A+           export NEXT_PUBLIC_APP_URL=https://app.plaivra.com%0A+           export NEXT_PUBLIC_USE_MOCK_AUTH=false%0A+           export PLAIVRA_MCP_BASE_URL=https://app.plaivra.com/api/mcp%0A+           export PLAIVRA_OAUTH_ISSUER=https://app.plaivra.com%0A+           export PLAIVRA_CIMD_ALLOWED_ORIGINS=https://chatgpt.com%0A+           export CRON_SECRET=ci-placeholder-cron-secret-that-is-long-enough%0A+           npm run validate:env%0A+%0A+       - name: Build production application%0A+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           export PLAIVRA_BUILD_TIMESTAMP="$(date -u +'%25Y-%25m-%25dT%25H:%25M:%25SZ')"%0A+           NEXT_PUBLIC_USE_MOCK_AUTH=false npm run build%0A+%0A+       - name: Install Playwright Chromium%0A+         if: steps.scope.outputs.ui == 'true'%0A+         run: npx playwright install --with-deps chromium%0A+%0A+       - name: Run relevant browser QA%0A+         if: steps.scope.outputs.ui == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           mkdir -p quality-reports%0A+           NEXT_PUBLIC_USE_MOCK_AUTH=true npm run dev > quality-reports/qa-server.log 2>&1 &%0A+           server_pid=$!%0A+           trap 'kill "$server_pid" 2>/dev/null || true' EXIT%0A+           for attempt in $(seq 1 60); do%0A+             if curl --fail --silent http://localhost:3000/ > /dev/null; then break; fi%0A+             sleep 1%0A+           done%0A+           curl --fail --silent http://localhost:3000/ > /dev/null%0A+%0A+           if [ "${{ steps.scope.outputs.train_ui }}" = "true" ]; then%0A+             QA_BASE_URL=http://localhost:3000 QA_TRAIN_EVIDENCE_DIR=quality-reports/train-qa-evidence npm run qa:train%0A+           else%0A+             QA_BASE_URL=http://localhost:3000 QA_EVIDENCE_DIR=quality-reports/rendered-qa-evidence npm run qa:rendered%0A+           fi%0A+%0A+       - name: Upload browser evidence on failure%0A+         if: failure() && steps.scope.outputs.ui == 'true'%0A+         uses: actions/upload-artifact@v4%0A+         with:%0A+           name: quality-failure-evidence-${{ github.run_id }}%0A+           path: quality-reports/%0A+           if-no-files-found: ignore%0A+           retention-days: 7%0A+%0A%0A ❯ lib/product/muscle-intelligence-phase1-migration.test.ts:101:29%0A%0A

::error file=/tmp/aw1a-main/lib/product/train-phase1-approved-ui.test.ts,title=lib/product/train-phase1-approved-ui.test.ts > approved Train Phase 1 UI contracts > keeps picker selection%2C duplicates%2C keyboard selection%2C focus return%2C request grouping%2C cancellation%2C and explicit pagination,line=42,column=20::AssertionError: expected '"use client";\n\nimport { Check, Dumb…' to contain 'className="absolute inset-0 rounded-2…'%0A%0A- Expected%0A+ Received%0A%0A- className="absolute inset-0 rounded-2xl%0A+ "use client";%0A+%0A+ import { Check, Dumbbell, ExternalLink, Eye, Plus, Search, X } from "lucide-react";%0A+ import { useEffect, useMemo, useRef, useState } from "react";%0A+ import { useAuth } from "@/components/auth/auth-provider";%0A+ import { ExerciseMusclePreview } from "@/components/workouts/exercise-muscle-preview";%0A+ import { Badge } from "@/components/ui/badge";%0A+ import { Button } from "@/components/ui/button";%0A+ import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";%0A+ import { Input } from "@/components/ui/input";%0A+ import { Select } from "@/components/ui/select-field";%0A+ import { CardGridSkeleton } from "@/components/ui/state-views";%0A+ import { userSafeError } from "@/lib/error-formatting";%0A+ import { useTrainTranslation } from "@/lib/i18n/train";%0A+ import { formatExerciseDisplayList, formatExerciseDisplayValue, type ExerciseDisplayDomain } from "@/lib/train/exercise-display";%0A+ import { getMuscleIntelligenceCopy } from "@/lib/train/muscle-intelligence/muscle-intelligence-ui-copy";%0A+ import { createCatalogRequestGroupId } from "@/services/activity-catalog/client";%0A+ import {%0A+   emptyCanonicalWorkoutFilterOptions,%0A+   getCanonicalWorkoutFilterOptionsWithStatus,%0A+   getWorkoutsWithStatus,%0A+   mergeCanonicalWorkoutFilterOptions,%0A+   type CanonicalWorkoutFilterOptions,%0A+   type WorkoutFilterOption%0A+ } from "@/services/database/workout-library";%0A+ import {%0A+   getWorkoutReplacementEligibility,%0A+   isReplacementCandidateActionable,%0A+   replacementEligibilityMessage,%0A+   shouldClosePickerAfterAdd,%0A+   type ReplacementEligibility%0A+ } from "@/services/database/workout-replacement-eligibility";%0A+ import type { Workout } from "@/types";%0A+%0A+ function normalizeIdentityPart(value: string | null | undefined) {%0A+   return value?.trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "") ?? "";%0A+ }%0A+%0A+ function exerciseKey(exercise: Pick<Workout, "id" | "name" | "target_muscle" | "catalog_slug">) {%0A+   if (exercise.catalog_slug) return `catalog:${exercise.catalog_slug}`;%0A+   if (exercise.id) return `id:${exercise.id}`;%0A+   return `legacy:${normalizeIdentityPart(exercise.name)}:${normalizeIdentityPart(exercise.target_muscle)}`;%0A+ }%0A+%0A+ function optionLabel(options: WorkoutFilterOption[], value: string, language: "en" | "de" | "ar", domain: ExerciseDisplayDomain) {%0A+   return options.find((option) => option.value === value)?.label ?? formatExerciseDisplayValue(value, language, domain);%0A+ }%0A+%0A+ function isAbortError(error: unknown) {%0A+   return Boolean(error && typeof error === "object" && "name" in error && (error as { name?: unknown }).name === "AbortError");%0A+ }%0A+%0A+ const emptyPagination = { hasMore: false, nextOffset: null as number | null };%0A+%0A+ export function ExercisePickerDialog({ open, onOpenChange, dayName, existingKeys, onAdd, maxSelection }: {%0A+   open: boolean;%0A+   onOpenChange: (open: boolean) => void;%0A+   dayName: string;%0A+   existingKeys: string[];%0A+   onAdd: (exercises: Workout[]) => void;%0A+   maxSelection?: number;%0A+ }) {%0A+   const { user } = useAuth();%0A+   const { language, dir, locale, tr } = useTrainTranslation();%0A+   const muscleCopy = getMuscleIntelligenceCopy(language);%0A+   const libraryLoadFailedMessage = tr("libraryLoadFailed");%0A+   const replacementMode = maxSelection === 1;%0A+   const [query, setQuery] = useState("");%0A+   const [muscle, setMuscle] = useState("");%0A+   const [equipment, setEquipment] = useState("");%0A+   const [difficulty, setDifficulty] = useState("");%0A+   const [muscleCategory, setMuscleCategory] = useState("");%0A+   const [secondaryMuscle, setSecondaryMuscle] = useState("");%0A+   const [forceType, setForceType] = useState("");%0A+   const [exerciseType, setExerciseType] = useState("");%0A+   const [mechanics, setMechanics] = useState("");%0A+   const [results, setResults] = useState<Workout[]>([]);%0A+   const [filterOptions, setFilterOptions] = useState<CanonicalWorkoutFilterOptions>(() => emptyCanonicalWorkoutFilterOptions());%0A+   const [selected, setSelected] = useState<Map<string, Workout>>(new Map());%0A+   const [focusedExercise, setFocusedExercise] = useState<Workout | null>(null);%0A+   const [eligibility, setEligibility] = useState<Map<string, ReplacementEligibility>>(new Map());%0A+   const [eligibilityLoading, setEligibilityLoading] = useState(false);%0A+   const [eligibilityError, setEligibilityError] = useState("");%0A+   const [loading, setLoading] = useState(false);%0A+   const [loadingMore, setLoadingMore] = useState(false);%0A+   const [error, setError] = useState("");%0A+   const [loadMoreError, setLoadMoreError] = useState("");%0A+   const [pagination, setPagination] = useState(emptyPagination);%0A+   const returnFocusRef = useRef<HTMLElement | null>(null);%0A+   const initialRequestGroupConsumedRef = useRef<string | null>(null);%0A+   const activeGenerationGroupRef = useRef<string | null>(null);%0A+   const generationRef = useRef(0);%0A+   const eligibilityGenerationRef = useRef(0);%0A+   const filtersControllerRef = useRef<AbortController | null>(null);%0A+   const activitiesControllerRef = useRef<AbortController | null>(null);%0A+   const loadMoreControllerRef = useRef<AbortController | null>(null);%0A+   const initialCatalogRequestGroupId = useMemo(() => open ? createCatalogRequestGroupId() : null, [open]);%0A+   const existing = useMemo(() => new Set(existingKeys), [existingKeys]);%0A+%0A+   const activeFilters = useMemo(() => ({%0A+     primaryMuscles: muscle ? [muscle] : [],%0A+     equipmentRequired: equipment ? [equipment] : [],%0A+     difficulty: difficulty || undefined,%0A+     muscleCategories: muscleCategory ? [muscleCategory] : [],%0A+     secondaryMuscles: secondaryMuscle ? [secondaryMuscle] : [],%0A+     forceTypes: forceType ? [forceType] : [],%0A+     exerciseTypes: exerciseType ? [exerciseType] : [],%0A+     mechanics: mechanics ? [mechanics] : []%0A+   }), [difficulty, equipment, exerciseType, forceType, mechanics, muscle, muscleCategory, secondaryMuscle]);%0A+%0A+   useEffect(() => {%0A+     if (open || typeof document === "undefined") return;%0A+     const captureReturnTarget = () => {%0A+       const activeElement = document.activeElement;%0A+       if (activeElement instanceof HTMLElement && activeElement !== document.body && !activeElement.closest("[data-train-exercise-picker]")) {%0A+         returnFocusRef.current = activeElement;%0A+       }%0A+     };%0A+     captureReturnTarget();%0A+     document.addEventListener("focusin", captureReturnTarget, true);%0A+     return () => document.removeEventListener("focusin", captureReturnTarget, true);%0A+   }, [open]);%0A+%0A+   useEffect(() => {%0A+     if (!open || !initialCatalogRequestGroupId) return;%0A+     filtersControllerRef.current?.abort();%0A+     const controller = new AbortController();%0A+     filtersControllerRef.current = controller;%0A+     void getCanonicalWorkoutFilterOptionsWithStatus(locale, {%0A+       requestGroupId: initialCatalogRequestGroupId,%0A+       signal: controller.signal%0A+     }).then((result) => {%0A+       if (!controller.signal.aborted) setFilterOptions(result.data);%0A+     }).catch(() => {%0A+       // Result-derived options remain available if filter metadata fails.%0A+     });%0A+     return () => controller.abort();%0A+   }, [initialCatalogRequestGroupId, locale, open]);%0A+%0A+   useEffect(() => {%0A+     if (!open) return;%0A+     const generation = generationRef.current + 1;%0A+     generationRef.current = generation;%0A+     activitiesControllerRef.current?.abort();%0A+     loadMoreControllerRef.current?.abort();%0A+     const controller = new AbortController();%0A+     activitiesControllerRef.current = controller;%0A+     const initialGroupId = initialCatalogRequestGroupId;%0A+     const useInitialGroup = initialGroupId !== null && initialRequestGroupConsumedRef.current !== initialGroupId;%0A+     const requestGroupId = useInitialGroup ? initialGroupId : createCatalogRequestGroupId();%0A+     activeGenerationGroupRef.current = requestGroupId;%0A+     const timer = window.setTimeout(() => {%0A+       if (useInitialGroup && initialGroupId) initialRequestGroupConsumedRef.current = initialGroupId;%0A+       setLoading(true);%0A+       setLoadingMore(false);%0A+       setError("");%0A+       setLoadMoreError("");%0A+       setPagination(emptyPagination);%0A+       getWorkoutsWithStatus(query.trim(), activeFilters, 0, locale, {%0A+         requestGroupId,%0A+         signal: controller.signal%0A+       })%0A+         .then((result) => {%0A+           if (controller.signal.aborted || generationRef.current !== generation) return;%0A+           setResults(result.data);%0A+           setPagination(result.pagination ?? emptyPagination);%0A+           setFocusedExercise((current) => current && result.data.some((workout) => exerciseKey(workout) === exerciseKey(current)) ? current : null);%0A+           if (result.filterOptions) setFilterOptions((options) => mergeCanonicalWorkoutFilterOptions(options, result.filterOptions!));%0A+         })%0A+         .catch((loadError) => {%0A+           if (controller.signal.aborted || generationRef.current !== generation || isAbortError(loadError)) return;%0A+           setResults([]);%0A+           setFocusedExercise(null);%0A+           setPagination(emptyPagination);%0A+           setError(userSafeError(loadError, libraryLoadFailedMessage));%0A+         })%0A+         .finally(() => {%0A+           if (!controller.signal.aborted && generationRef.current === generation) setLoading(false);%0A+         });%0A+     }, 180);%0A+     return () => {%0A+       window.clearTimeout(timer);%0A+       controller.abort();%0A+     };%0A+   }, [activeFilters, initialCatalogRequestGroupId, libraryLoadFailedMessage, locale, open, query]);%0A+%0A+   useEffect(() => {%0A+     if (!open || !replacementMode) {%0A+       setEligibility(new Map());%0A+       setEligibilityLoading(false);%0A+       setEligibilityError("");%0A+       return;%0A+     }%0A+     const generation = eligibilityGenerationRef.current + 1;%0A+     eligibilityGenerationRef.current = generation;%0A+     if (!user?.id) {%0A+       setEligibility(new Map());%0A+       setEligibilityLoading(false);%0A+       setEligibilityError("Sign in again to verify replacement eligibility.");%0A+       return;%0A+     }%0A+     if (!results.length) {%0A+       setEligibility(new Map());%0A+       setEligibilityLoading(false);%0A+       setEligibilityError("");%0A+       return;%0A+     }%0A+     setEligibilityLoading(true);%0A+     setEligibilityError("");%0A+     void getWorkoutReplacementEligibility(%0A+       user.id,%0A+       results.map((workout) => ({ key: exerciseKey(workout), workout }))%0A+     ).then((next) => {%0A+       if (eligibilityGenerationRef.current === generation) setEligibility(next);%0A+     }).catch((loadError) => {%0A+       if (eligibilityGenerationRef.current !== generation) return;%0A+       setEligibility(new Map());%0A+       setEligibilityError(userSafeError(loadError, "Replacement eligibility could not be verified. Your current exercise was preserved."));%0A+     }).finally(() => {%0A+       if (eligibilityGenerationRef.current === generation) setEligibilityLoading(false);%0A+     });%0A+   }, [open, replacementMode, results, user?.id]);%0A+%0A+   useEffect(() => {%0A+     if (open) return;%0A+     filtersControllerRef.current?.abort();%0A+     activitiesControllerRef.current?.abort();%0A+     loadMoreControllerRef.current?.abort();%0A+     generationRef.current += 1;%0A+     eligibilityGenerationRef.current += 1;%0A+     initialRequestGroupConsumedRef.current = null;%0A+     activeGenerationGroupRef.current = null;%0A+     setSelected(new Map());%0A+     setFocusedExercise(null);%0A+     setResults([]);%0A+     setEligibility(new Map());%0A+     setEligibilityLoading(false);%0A+     setEligibilityError("");%0A+     setPagination(emptyPagination);%0A+     setLoading(false);%0A+     setLoadingMore(false);%0A+     setError("");%0A+     setLoadMoreError("");%0A+   }, [open]);%0A+%0A+   const muscleOptions = filterOptions.primaryMuscles;%0A+   const equipmentOptions = filterOptions.equipmentRequired;%0A+   const difficultyOptions = filterOptions.experienceLevels;%0A+   const muscleCategoryOptions = filterOptions.muscleCategories;%0A+   const secondaryMuscleOptions = filterOptions.secondaryMuscles;%0A+   const forceTypeOptions = filterOptions.forceTypes;%0A+   const exerciseTypeOptions = filterOptions.exerciseTypes;%0A+   const mechanicsOptions = filterOptions.mechanics;%0A+   const hasAdvancedOptions = Boolean(muscleCategoryOptions.length || secondaryMuscleOptions.length || forceTypeOptions.length || exerciseTypeOptions.length || mechanicsOptions.length);%0A+%0A+   function isActionable(key: string) {%0A+     if (!replacementMode) return true;%0A+     return isReplacementCandidateActionable(eligibility.get(key), eligibilityLoading, eligibilityError);%0A+   }%0A+%0A+   function toggle(exercise: Workout) {%0A+     const key = exerciseKey(exercise);%0A+     if (existing.has(key) || !isActionable(key)) return;%0A+     setSelected((current) => {%0A+       if (current.has(key)) {%0A+         const next = new Map(current);%0A+         next.delete(key);%0A+         return next;%0A+       }%0A+       const next = maxSelection === 1 ? new Map<string, Workout>() : new Map(current);%0A+       next.set(key, exercise);%0A+       return next;%0A+     });%0A+   }%0A+%0A+   function addSelected() {%0A+     const chosen = Array.from(selected.values());%0A+     if (!chosen.length) return;%0A+     onAdd(chosen);%0A+     if (shouldClosePickerAfterAdd(replacementMode)) onOpenChange(false);%0A+   }%0A+%0A+   function clearFilters() {%0A+     setQuery("");%0A+     setMuscle("");%0A+     setEquipment("");%0A+     setDifficulty("");%0A+     setMuscleCategory("");%0A+     setSecondaryMuscle("");%0A+     setForceType("");%0A+     setExerciseType("");%0A+     setMechanics("");%0A+   }%0A+%0A+   async function loadMore() {%0A+     const nextOffset = pagination.nextOffset;%0A+     if (!pagination.hasMore || nextOffset === null || loadingMore) return;%0A+     loadMoreControllerRef.current?.abort();%0A+     const controller = new AbortController();%0A+     loadMoreControllerRef.current = controller;%0A+     const generation = generationRef.current;%0A+     const requestGroupId = activeGenerationGroupRef.current ?? createCatalogRequestGroupId();%0A+     activeGenerationGroupRef.current = requestGroupId;%0A+     setLoadingMore(true);%0A+     setLoadMoreError("");%0A+     try {%0A+       const result = await getWorkoutsWithStatus(query.trim(), activeFilters, nextOffset, locale, {%0A+         requestGroupId,%0A+         signal: controller.signal%0A+       });%0A+       if (controller.signal.aborted || generationRef.current !== generation) return;%0A+       setResults((current) => {%0A+         const keys = new Set(current.map(exerciseKey));%0A+         return [...current, ...result.data.filter((exercise) => {%0A+           const key = exerciseKey(exercise);%0A+           if (keys.has(key)) return false;%0A+           keys.add(key);%0A+           return true;%0A+         })];%0A+       });%0A+       setPagination(result.pagination ?? emptyPagination);%0A+       if (result.filterOptions) setFilterOptions((options) => mergeCanonicalWorkoutFilterOptions(options, result.filterOptions!));%0A+     } catch (loadError) {%0A+       if (controller.signal.aborted || generationRef.current !== generation || isAbortError(loadError)) return;%0A+       setLoadMoreError(userSafeError(loadError, tr("moreExercisesLoadFallback")));%0A+     } finally {%0A+       if (!controller.signal.aborted && generationRef.current === generation) setLoadingMore(false);%0A+     }%0A+   }%0A+%0A+   const hasFilters = Boolean(query || muscle || equipment || difficulty || muscleCategory || secondaryMuscle || forceType || exerciseType || mechanics);%0A+   const activeFilterChips = [%0A+     muscle ? { id: "muscle", label: optionLabel(muscleOptions, muscle, language, "muscle"), clear: () => setMuscle("") } : null,%0A+     equipment ? { id: "equipment", label: optionLabel(equipmentOptions, equipment, language, "equipment"), clear: () => setEquipment("") } : null,%0A+     difficulty ? { id: "difficulty", label: optionLabel(difficultyOptions, difficulty, language, "difficulty"), clear: () => setDifficulty("") } : null,%0A+     muscleCategory ? { id: "muscle-category", label: optionLabel(muscleCategoryOptions, muscleCategory, language, "muscle"), clear: () => setMuscleCategory("") } : null,%0A+     secondaryMuscle ? { id: "secondary-muscle", label: optionLabel(secondaryMuscleOptions, secondaryMuscle, language, "muscle"), clear: () => setSecondaryMuscle("") } : null,%0A+     forceType ? { id: "force-type", label: optionLabel(forceTypeOptions, forceType, language, "force"), clear: () => setForceType("") } : null,%0A+     exerciseType ? { id: "exercise-type", label: optionLabel(exerciseTypeOptions, exerciseType, language, "category"), clear: () => setExerciseType("") } : null,%0A+     mechanics ? { id: "mechanics", label: optionLabel(mechanicsOptions, mechanics, language, "movement"), clear: () => setMechanics("") } : null%0A+   ].filter((chip): chip is { id: string; label: string; clear: () => void } => Boolean(chip));%0A+%0A+   return (%0A+     <Dialog open={open} onOpenChange={onOpenChange}>%0A+       <DialogContent%0A+         layout="responsive-drawer"%0A+         className="inset-x-0 bottom-0 top-0 h-dvh max-h-dvh w-screen max-w-none rounded-none border-0 sm:inset-x-0 sm:bottom-0 sm:top-0 sm:h-dvh sm:max-h-dvh sm:w-screen sm:max-w-none sm:translate-x-0 sm:translate-y-0 sm:rounded-none sm:p-0 lg:inset-y-0 lg:left-auto lg:right-0 lg:top-0 lg:h-dvh lg:max-h-dvh lg:w-[min(54rem,100vw)] lg:max-w-[54rem] lg:translate-x-0 lg:translate-y-0 lg:rounded-none lg:border-y-0 lg:border-l lg:rtl:left-0 lg:rtl:right-auto lg:rtl:border-l-0 lg:rtl:border-r"%0A+         closeLabel={tr("closePicker")}%0A+         dir={dir}%0A+         data-train-exercise-picker%0A+         onOpenAutoFocus={() => {%0A+           if (returnFocusRef.current?.isConnected) return;%0A+           const activeElement = document.activeElement;%0A+           if (activeElement instanceof HTMLElement && !activeElement.closest("[data-train-exercise-picker]")) returnFocusRef.current = activeElement;%0A+         }}%0A+         onCloseAutoFocus={(event) => {%0A+           const returnTarget = returnFocusRef.current;%0A+           if (!returnTarget?.isConnected) return;%0A+           event.preventDefault();%0A+           returnTarget.focus();%0A+           window.requestAnimationFrame(() => {%0A+             if (document.activeElement !== returnTarget) returnTarget.focus();%0A+           });%0A+         }}%0A+       >%0A+         <DialogHeader className="mb-0 shrink-0 border-b px-5 pb-4 pt-[calc(env(safe-area-inset-top)+1rem)] pe-16 text-start lg:pt-4">%0A+           <DialogTitle className="text-xl">{tr("addExercisesTo", { day: dayName })}</DialogTitle>%0A+           <DialogDescription className="font-medium text-foreground/70">{tr("selectionPreserved", { count: selected.size })}</DialogDescription>%0A+         </DialogHeader>%0A+%0A+         <div className="min-h-0 flex-1 overflow-y-auto overscroll-contain px-4 py-4 pb-32 sm:px-5" data-picker-scroll-region>%0A+           <div className="sticky -top-4 z-20 -mx-4 flex gap-2 overflow-x-auto border-b bg-background/95 px-4 py-3 backdrop-blur sm:-mx-5 sm:px-5 lg:grid lg:grid-cols-4 lg:gap-3 lg:overflow-visible" data-picker-filters>%0A+             <label className="relative min-w-[240px] shrink-0 lg:min-w-0"><span className="sr-only">{tr("searchExercises")}</span><Search className="pointer-events-none absolute start-3 top-3.5 h-4 w-4 text-muted-foreground" /><Input value={query} onChange={(event) => setQuery(event.target.value)} className="min-h-12 ps-10" placeholder={tr("searchExercises")} /></label>%0A+             <div className="min-w-[180px] shrink-0 lg:min-w-0"><Select className="h-12 w-full rounded-full border border-input bg-card px-4 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring" aria-label={tr("primaryMuscle")} value={muscle} onChange={setMuscle} placeholder={tr("primaryMuscle")} options={muscleOptions} /></div>%0A+             <div className="min-w-[180px] shrink-0 lg:min-w-0"><Select className="h-12 w-full rounded-full border border-input bg-card px-4 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring" aria-label={tr("equipment")} value={equipment} onChange={setEquipment} placeholder={tr("equipment")} options={equipmentOptions} /></div>%0A+             <div className="min-w-[180px] shrink-0 lg:min-w-0"><Select className="h-12 w-full rounded-full border border-input bg-card px-4 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring" aria-label={tr("difficulty")} value={difficulty} onChange={setDifficulty} placeholder={tr("difficulty")} options={difficultyOptions} /></div>%0A+             {hasFilters ? <Button type="button" variant="ghost" className="min-h-11 justify-self-start px-2 sm:col-span-2 lg:col-span-4" onClick={clearFilters}>{tr("clear")}</Button> : null}%0A+           </div>%0A+%0A+           {activeFilterChips.length ? <div className="mt-3 flex gap-2 overflow-x-auto" aria-label={tr("filters")}>{activeFilterChips.map((chip) => <button key={chip.id} type="button" className="inline-flex min-h-11 shrink-0 items-center gap-2 rounded-full border border-primary/30 bg-primary/5 px-3 text-sm font-medium" onClick={chip.clear}>{chip.label}<X className="h-4 w-4" aria-hidden="true" /></button>)}</div> : null}%0A+%0A+           {hasAdvancedOptions ? <details className="mt-3 rounded-2xl border border-border/70 bg-muted/20 px-3 py-2"><summary className="min-h-11 cursor-pointer select-none content-center font-medium focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring">{tr("moreFilters")}</summary><div className="mt-3 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{muscleCategoryOptions.length ? <Select aria-label={tr("muscleCategory")} value={muscleCategory} onChange={setMuscleCategory} placeholder={tr("muscleCategory")} options={muscleCategoryOptions} /> : null}{secondaryMuscleOptions.length ? <Select aria-label={tr("secondaryMuscle")} value={secondaryMuscle} onChange={setSecondaryMuscle} placeholder={tr("secondaryMuscle")} options={secondaryMuscleOptions} /> : null}{forceTypeOptions.length ? <Select aria-label={tr("forceType")} value={forceType} onChange={setForceType} placeholder={tr("forceType")} options={forceTypeOptions} /> : null}{exerciseTypeOptions.length ? <Select aria-label={tr("exerciseType")} value={exerciseType} onChange={setExerciseType} placeholder={tr("exerciseType")} options={exerciseTypeOptions} /> : null}{mechanicsOptions.length ? <Select aria-label={tr("mechanics")} value={mechanics} onChange={setMechanics} placeholder={tr("mechanics")} options={mechanicsOptions} /> : null}</div></details> : null}%0A+%0A+           <div className="mt-4"><ExerciseMusclePreview exercise={focusedExercise} language={language} /></div>%0A+           {loading ? <div className="mt-4"><CardGridSkeleton count={4} rows={3} /></div> : null}%0A+           {error ? <div role="alert" className="mt-4 rounded-2xl border border-destructive/30 bg-destructive/5 p-4 text-sm">{error}</div> : null}%0A+           {replacementMode && eligibilityError ? <div role="alert" className="mt-4 rounded-2xl border border-destructive/30 bg-destructive/5 p-4 text-sm">{eligibilityError}</div> : null}%0A+%0A+           {!loading && !error ? <><div className="mt-4 grid grid-cols-1 gap-3 lg:grid-cols-2" data-picker-results>{results.map((exercise) => {%0A+             const key = exerciseKey(exercise);%0A+             const duplicate = existing.has(key);%0A+             const isSelected = selected.has(key);%0A+             const isFocused = focusedExercise ? exerciseKey(focusedExercise) === key : false;%0A+             const candidateEligibility = eligibility.get(key);%0A+             const candidatePending = replacementMode && eligibilityLoading;%0A+             const ineligible = replacementMode && !candidatePending && candidateEligibility?.eligible !== true;%0A+             const disabled = duplicate || candidatePending || ineligible || Boolean(replacementMode && eligibilityError);%0A+             const guideUrl = exercise.custom_video_url || exercise.video_url || exercise.exercise_url;%0A+             return <article key={key} className={`flex min-h-[170px] flex-col rounded-2xl border p-4 ${disabled ? "border-border/60 bg-muted/30" : isSelected ? "border-primary bg-primary/5 ring-1 ring-primary/20" : isFocused ? "border-primary/50 bg-card" : "bg-card"}`} data-picker-exercise-card><div className="flex min-w-0 items-start justify-between gap-3"><div className="min-w-0"><h3 className="break-words text-base font-semibold leading-6">{exercise.name}</h3><p className="mt-1 break-words text-sm text-muted-foreground">{formatExerciseDisplayList(exercise.target_muscle, language, "muscle")} · {formatExerciseDisplayList(exercise.equipment, language, "equipment")}</p></div>{exercise.difficulty ? <Badge variant="outline" className="shrink-0">{formatExerciseDisplayValue(exercise.difficulty, language, "difficulty")}</Badge> : null}</div><p className="mt-3 line-clamp-2 text-sm leading-5 text-muted-foreground">{exercise.instructions}</p>{replacementMode && candidatePending ? <p className="mt-2 text-xs text-muted-foreground">Verifying tracked replacement eligibility…</p> : null}{replacementMode && ineligible ? <p className="mt-2 text-xs text-muted-foreground">{replacementEligibilityMessage(candidateEligibility?.reason)}</p> : null}<div className="mt-auto grid gap-2 pt-4 sm:grid-cols-2"><Button type="button" variant={isSelected ? "default" : "outline"} className="min-h-11 w-full" disabled={disabled} aria-pressed={isSelected} onClick={() => toggle(exercise)}>{duplicate || isSelected ? <Check className="h-4 w-4" /> : <Plus className="h-4 w-4" />}{duplicate ? tr("alreadyAdded") : isSelected ? tr("deselect") : ineligible ? "Unavailable" : candidatePending ? "Checking…" : tr("select")}</Button><Button type="button" variant={isFocused ? "default" : "outline"} className="min-h-11 w-full" aria-pressed={isFocused} onClick={() => setFocusedExercise(exercise)}><Eye className="h-4 w-4" />{muscleCopy.previewAction}</Button>{guideUrl ? <Button asChild type="button" variant="ghost" className="min-h-11 sm:col-span-2"><a href={guideUrl} target="_blank" rel="noreferrer"><ExternalLink className="h-4 w-4" />{tr("viewGuide")}</a></Button> : null}</div></article>;%0A+           })}</div>{results.length && (pagination.hasMore || loadMoreError) ? <div className="mt-5 flex flex-col items-center gap-3" data-picker-load-more>{loadMoreError ? <div role="alert" className="w-full rounded-2xl border border-destructive/30 bg-destructive/5 p-3 text-sm">{loadMoreError}</div> : null}{pagination.hasMore ? <Button type="button" variant="outline" className="min-h-12 min-w-40" disabled={loadingMore} onClick={() => void loadMore()}>{loadingMore ? tr("loadingLabel") : tr("loadMore")}</Button> : null}</div> : null}</> : null}%0A+           {!loading && !error && !results.length ? <div className="mt-4 grid min-h-40 place-items-center rounded-2xl border border-dashed text-center"><div><Dumbbell className="mx-auto mb-2 h-6 w-6 text-muted-foreground" /><p className="font-medium">{tr("noExercisesFound")}</p><p className="text-sm text-muted-foreground">{tr("broaderSearch")}</p></div></div> : null}%0A+         </div>%0A+%0A+         <div className="absolute inset-x-0 bottom-0 z-30 border-t bg-background/95 px-4 py-3 pb-[calc(env(safe-area-inset-bottom)+1rem)] backdrop-blur sm:px-5" data-picker-footer><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex min-h-11 items-center gap-2"><span className="text-sm font-semibold">{tr("selectedCount", { count: selected.size })}</span><Button type="button" variant="ghost" className="min-h-11" onClick={() => setSelected(new Map())} disabled={!selected.size}>{tr("clear")}</Button></div><Button type="button" className="min-h-[52px] w-full sm:w-auto sm:min-w-44" onClick={addSelected} disabled={!selected.size || (replacementMode && (eligibilityLoading || Boolean(eligibilityError)))}>{tr("addNExercises", { count: selected.size })}</Button></div></div>%0A+       </DialogContent>%0A+     </Dialog>%0A+   );%0A+ }%0A+%0A+ export { exerciseKey };%0A+%0A%0A ❯ lib/product/train-phase1-approved-ui.test.ts:42:20%0A%0A

::error file=/tmp/aw1a-main/lib/product/train-phase1-approved-ui.test.ts,title=lib/product/train-phase1-approved-ui.test.ts > approved Train Phase 1 UI contracts > localizes detail%2C history filters%2C direct-session failures%2C and the active workout controller,line=86,column=20::AssertionError: expected '"use client";\n\nimport { AlertTriang…' to contain 'const metadata = metadataLine(item.ta…'%0A%0A- Expected%0A+ Received%0A%0A- const metadata = metadataLine(item.target_muscle, item.equipment)%0A+ "use client";%0A+%0A+ import { AlertTriangle, CheckCircle2, ExternalLink, Heart, Play, RotateCcw, Save, TrendingUp, ChevronDown } from "lucide-react";%0A+ import { useParams } from "next/navigation";%0A+ import { useCallback, useEffect, useMemo, useState } from "react";%0A+ import { Button } from "@/components/ui/button";%0A+ import { Badge } from "@/components/ui/badge";%0A+ import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";%0A+ import { Input } from "@/components/ui/input";%0A+ import { Label } from "@/components/ui/label";%0A+ import { CardSkeleton, ErrorState } from "@/components/ui/state-views";%0A+ import { PageHeading } from "@/components/layout/page-heading";%0A+ import { TrainPageContainer } from "@/components/workouts/train-ui";%0A+ import { ExerciseVideoPlayer } from "@/components/workouts/video-player";%0A+ import { useAuth } from "@/components/auth/auth-provider";%0A+ import { useToast } from "@/components/ui/toaster";%0A+ import Link from "next/link";%0A+ import { getExerciseVideos, getUserExerciseVideo, getWorkout, getWorkoutAlternatives, resetUserExerciseVideo, upsertUserExerciseVideo } from "@/services/database/workout-library";%0A+ import { getWorkoutHistoryDetailed } from "@/services/database/workout-sessions";%0A+ import { getCustomExercisesWithStatus, getFavoriteExerciseIdsWithStatus, setFavoriteExercise } from "@/services/workouts/exercise-library-store";%0A+ import { findExerciseVideo } from "@/services/workouts/video-matching";%0A+ import { userSafeError } from "@/lib/error-formatting";%0A+ import { cn } from "@/lib/utils";%0A+ import { useTrainTranslation } from "@/lib/i18n/train";%0A+ import { formatExerciseDisplayList, formatExerciseDisplayValue } from "@/lib/train/exercise-display";%0A+ import type { ExerciseLog, ExerciseVideo, Workout, WorkoutSessionSummary } from "@/types";%0A+%0A+ function normalizeExerciseName(value: string) {%0A+   return value.toLowerCase().replace(/^[a-z]\d\s*[:.)-]\s*/i, "").replace(/[^a-z0-9]+/g, " ").trim();%0A+ }%0A+%0A+ function estimateOneRepMax(weightKg: number, reps: number) {%0A+   if (weightKg <= 0 || reps <= 0) return 0;%0A+   return Math.round(weightKg * (1 + reps / 30) * 10) / 10;%0A+ }%0A+%0A+ function round(value: number) {%0A+   return Math.round(value * 10) / 10;%0A+ }%0A+%0A+ function exerciseLogsFor(history: WorkoutSessionSummary[], workout: Pick<Workout, "id" | "name">) {%0A+   const target = normalizeExerciseName(workout.name);%0A+   return history.flatMap((session) =>%0A+     (session.exercise_logs ?? [])%0A+       .filter((log) => {%0A+         if (log.source_workout_id) return log.source_workout_id === workout.id;%0A+         if (session.workout_id) return session.workout_id === workout.id;%0A+         return normalizeExerciseName(log.exercise_name) === target;%0A+       })%0A+       .map((log) => ({ ...log, sessionDate: session.completed_at || session.started_at }))%0A+   );%0A+ }%0A+%0A+ function buildExerciseHistory(logs: Array<ExerciseLog & { sessionDate: string }>) {%0A+   const valid = logs.filter((log) => Number(log.reps ?? 0) > 0 || Number(log.weight_kg ?? 0) > 0);%0A+   const bestWeight = Math.max(0, ...valid.map((log) => Number(log.weight_kg ?? 0)));%0A+   const bestReps = Math.max(0, ...valid.map((log) => Number(log.reps ?? 0)));%0A+   const bestOneRepMax = Math.max(0, ...valid.map((log) => estimateOneRepMax(Number(log.weight_kg ?? 0), Number(log.reps ?? 0))));%0A+   const bestSet = [...valid].sort((a, b) => (Number(b.weight_kg ?? 0) * Number(b.reps ?? 0)) - (Number(a.weight_kg ?? 0) * Number(a.reps ?? 0)))[0] ?? null;%0A+   const volumeByDate = new Map<string, number>();%0A+   const bestSetByDate = new Map<string, number>();%0A+   const oneRmByDate = new Map<string, number>();%0A+%0A+   valid.forEach((log) => {%0A+     const date = log.sessionDate.slice(0, 10);%0A+     const volume = Number(log.weight_kg ?? 0) * Number(log.reps ?? 0);%0A+     const oneRm = estimateOneRepMax(Number(log.weight_kg ?? 0), Number(log.reps ?? 0));%0A+     volumeByDate.set(date, (volumeByDate.get(date) ?? 0) + volume);%0A+     bestSetByDate.set(date, Math.max(bestSetByDate.get(date) ?? 0, volume));%0A+     oneRmByDate.set(date, Math.max(oneRmByDate.get(date) ?? 0, oneRm));%0A+   });%0A+%0A+   const trends = Array.from(volumeByDate.entries()).sort(([a], [b]) => a.localeCompare(b)).map(([date, volume]) => ({%0A+     date,%0A+     volume: round(volume),%0A+     bestSetVolume: round(bestSetByDate.get(date) ?? 0),%0A+     estimatedOneRepMax: round(oneRmByDate.get(date) ?? 0)%0A+   }));%0A+%0A+   return { count: valid.length, bestWeight, bestReps, bestOneRepMax: round(bestOneRepMax), bestSet, trends };%0A+ }%0A+%0A+ function sameText(a: string | null | undefined, b: string | null | undefined) {%0A+   const left = normalizeExerciseName(a ?? "");%0A+   const right = normalizeExerciseName(b ?? "");%0A+   return Boolean(left && right && left === right);%0A+ }%0A+%0A+ function hasInvalidUrl(value: string | null | undefined) {%0A+   const clean = value?.trim();%0A+   return Boolean(clean && !/^https?:\/\/[^\s]+$/i.test(clean));%0A+ }%0A+%0A+ function metadataLine(...values: Array<string | null | undefined>) {%0A+   return values.map((value) => value?.trim()).filter(Boolean).join(" · ");%0A+ }%0A+%0A+ export default function WorkoutDetailsPage() {%0A+   const params = useParams<{ id: string }>();%0A+   const { user } = useAuth();%0A+   const userId = user?.id;%0A+   const { toast } = useToast();%0A+   const { language, dir, locale, tr } = useTrainTranslation();%0A+   const [workout, setWorkout] = useState<Workout | null>(null);%0A+   const [video, setVideo] = useState<ExerciseVideo | null>(null);%0A+   const [customVideoUrl, setCustomVideoUrl] = useState("");%0A+   const [customVideoDraft, setCustomVideoDraft] = useState("");%0A+   const [isSavingVideo, setIsSavingVideo] = useState(false);%0A+   const [loadError, setLoadError] = useState("");%0A+   const [loadWarning, setLoadWarning] = useState("");%0A+   const [isLoading, setIsLoading] = useState(true);%0A+   const [history, setHistory] = useState<WorkoutSessionSummary[]>([]);%0A+   const [alternatives, setAlternatives] = useState<Workout[]>([]);%0A+   const [favoriteIds, setFavoriteIds] = useState<string[]>([]);%0A+   const [isFavoritePending, setIsFavoritePending] = useState(false);%0A+   const [favoriteError, setFavoriteError] = useState("");%0A+   const [videoStatus, setVideoStatus] = useState<"idle" | "saving" | "saved" | "failed" | "reset">("idle");%0A+   const [videoMessage, setVideoMessage] = useState("");%0A+%0A+   const load = useCallback(async () => {%0A+     setIsLoading(true);%0A+     setLoadError("");%0A+     setLoadWarning("");%0A+     try {%0A+       const warnings: string[] = [];%0A+       const customResult = await getCustomExercisesWithStatus(userId);%0A+       if (customResult.status.source === "degraded" && customResult.status.message) warnings.push(customResult.status.message);%0A+       const customExercise = customResult.data.find((exercise) => exercise.id === params.id) ?? null;%0A+       const nextWorkout = customExercise ?? await getWorkout(params.id, locale);%0A+       const [videosResult, customVideoResult, historyResult, alternativesResult, favoritesResult] = await Promise.allSettled([%0A+         getExerciseVideos(nextWorkout.name, locale),%0A+         userId && !customExercise ? getUserExerciseVideo(userId, nextWorkout.id) : Promise.resolve(null),%0A+         userId ? getWorkoutHistoryDetailed(userId, 150) : Promise.resolve([]),%0A+         getWorkoutAlternatives(nextWorkout.id, 6, locale),%0A+         getFavoriteExerciseIdsWithStatus(userId)%0A+       ]);%0A+       const videos = videosResult.status === "fulfilled" ? videosResult.value : [];%0A+       if (videosResult.status === "rejected") warnings.push(tr("exerciseVideoLoadWarning"));%0A+       const customVideo = customVideoResult.status === "fulfilled" ? customVideoResult.value : null;%0A+       if (customVideoResult.status === "rejected") warnings.push(tr("customVideoLoadWarning"));%0A+       const workoutHistory = historyResult.status === "fulfilled" ? historyResult.value : [];%0A+       if (historyResult.status === "rejected") warnings.push(tr("exerciseHistoryLoadWarning"));%0A+       const libraryAlternatives = alternativesResult.status === "fulfilled" ? alternativesResult.value.data : [];%0A+       if (alternativesResult.status === "fulfilled" && alternativesResult.value.status.message) warnings.push(alternativesResult.value.status.message);%0A+       if (alternativesResult.status === "rejected") warnings.push(tr("alternativesLoadWarning"));%0A+       if (favoritesResult.status === "fulfilled") {%0A+         setFavoriteIds(favoritesResult.value.data);%0A+         if (favoritesResult.value.status.source === "degraded" && favoritesResult.value.status.message) warnings.push(favoritesResult.value.status.message);%0A+       } else {%0A+         warnings.push(tr("favoriteLoadWarning"));%0A+         setFavoriteIds([]);%0A+       }%0A+       const localCustomAlternatives = customResult.data%0A+         .filter((item) => item.id !== nextWorkout.id)%0A+         .filter((item) => sameText(item.target_muscle, nextWorkout.target_muscle) || sameText(item.equipment, nextWorkout.equipment) || sameText(item.mechanics, nextWorkout.mechanics));%0A+       const combinedAlternatives = [...localCustomAlternatives, ...libraryAlternatives]%0A+         .filter((item) => item.id !== nextWorkout.id)%0A+         .filter((item, index, all) => all.findIndex((match) => match.id === item.id) === index)%0A+         .slice(0, 6);%0A+       setWorkout(nextWorkout);%0A+       setVideo(findExerciseVideo(nextWorkout, videos));%0A+       setCustomVideoUrl(customExercise?.custom_video_url ?? customVideo?.custom_video_url ?? "");%0A+       setCustomVideoDraft(customExercise?.custom_video_url ?? customVideo?.custom_video_url ?? "");%0A+       setHistory(workoutHistory);%0A+       setAlternatives(combinedAlternatives);%0A+       setLoadWarning(Array.from(new Set(warnings)).join(" "));%0A+     } catch (error) {%0A+       const message = userSafeError(error, tr("workoutDetailsLoadFailed"));%0A+       setLoadError(message);%0A+       toast({ title: tr("couldNotLoadWorkout"), description: message });%0A+     } finally {%0A+       setIsLoading(false);%0A+     }%0A+   }, [locale, params.id, toast, tr, userId]);%0A+%0A+   useEffect(() => {%0A+     void load();%0A+   }, [load]);%0A+%0A+   const exerciseHistory = useMemo(() => workout ? buildExerciseHistory(exerciseLogsFor(history, workout)) : null, [history, workout]);%0A+%0A+   if (isLoading) return <CardSkeleton rows={7} />;%0A+%0A+   if (loadError) {%0A+     return (%0A+       <ErrorState%0A+         title={tr("planUnavailable")}%0A+         description={loadError}%0A+         onRetry={load}%0A+         fallbackLabel={tr("exerciseLibrary")}%0A+         fallbackHref="/workouts"%0A+       />%0A+     );%0A+   }%0A+%0A+   if (!workout) {%0A+     return (%0A+       <ErrorState%0A+         title={tr("workoutNotFound")}%0A+         description={tr("exerciseNotFoundDescription")}%0A+         fallbackLabel={tr("exerciseLibrary")}%0A+         fallbackHref="/workouts"%0A+       />%0A+     );%0A+   }%0A+%0A+   const guideUrl = workout.exercise_url || (workout.notes?.startsWith("http") ? workout.notes : video?.exercise_url);%0A+   const videoUrl = customVideoUrl || null;%0A+   const displayVideo = video%0A+     ? { ...video, exercise_url: guideUrl ?? video.exercise_url, video_url: customVideoUrl || video.video_url }%0A+     : videoUrl%0A+       ? { id: workout.id, exercise_name: workout.name, category_type: workout.category, category: workout.target_muscle, exercise_url: guideUrl ?? "", video_url: customVideoUrl, instructions: workout.instructions, source: "user_custom", is_global: true }%0A+       : null;%0A+   const secondaryMuscles = workout.secondary_muscles ?? video?.secondary_muscles ?? [];%0A+   const displayTarget = formatExerciseDisplayList(workout.target_muscle || workout.muscle_category, language, "muscle");%0A+   const displayEquipment = formatExerciseDisplayList(workout.equipment_required || workout.equipment, language, "equipment");%0A+   const displayDifficulty = formatExerciseDisplayValue(workout.experience_level || workout.difficulty, language, "difficulty");%0A+   const displayMovement = formatExerciseDisplayValue(workout.movement_pattern || workout.mechanics || workout.category, language, "movement");%0A+   const displayForce = formatExerciseDisplayValue(workout.force_type, language, "force");%0A+   const displaySecondary = formatExerciseDisplayList(secondaryMuscles, language, "muscle");%0A+   const favorite = favoriteIds.includes(workout.id);%0A+   const mistakes = workout.notes && !workout.notes.startsWith("http") && /mistake|avoid|form/i.test(workout.notes) ? workout.notes : tr("noneSaved");%0A+   const customVideoInvalid = hasInvalidUrl(customVideoDraft);%0A+%0A+   async function saveCustomVideo() {%0A+     if (!workout) return;%0A+     if (!userId) {%0A+       const message = tr("customVideoSignIn");%0A+       setVideoStatus("failed");%0A+       setVideoMessage(message);%0A+       toast({ title: tr("signInRequired"), description: message });%0A+       return;%0A+     }%0A+     if (customVideoInvalid) {%0A+       setVideoStatus("failed");%0A+       setVideoMessage(tr("invalidCustomVideoUrl"));%0A+       return;%0A+     }%0A+     try {%0A+       setIsSavingVideo(true);%0A+       setVideoStatus("saving");%0A+       setVideoMessage("");%0A+       const saved = await upsertUserExerciseVideo(userId, workout.id, customVideoDraft);%0A+       setCustomVideoUrl(saved.custom_video_url);%0A+       setCustomVideoDraft(saved.custom_video_url);%0A+       setVideoStatus("saved");%0A+       setVideoMessage(tr("customVideoSavedMessage"));%0A+       toast({ title: tr("customVideoSavedTitle"), description: tr("customVideoSavedDescription") });%0A+     } catch (error) {%0A+       const message = userSafeError(error, tr("checkVideoLink"));%0A+       setVideoStatus("failed");%0A+       setVideoMessage(message);%0A+       toast({ title: tr("customVideoSaveFailed"), description: message });%0A+     } finally {%0A+       setIsSavingVideo(false);%0A+     }%0A+   }%0A+%0A+   async function resetCustomVideo() {%0A+     if (!workout || !userId) return;%0A+     try {%0A+       setIsSavingVideo(true);%0A+       setVideoStatus("saving");%0A+       setVideoMessage("");%0A+       await resetUserExerciseVideo(userId, workout.id);%0A+       setCustomVideoUrl("");%0A+       setCustomVideoDraft("");%0A+       setVideoStatus("reset");%0A+       setVideoMessage(tr("customVideoClearedMessage"));%0A+       toast({ title: tr("customVideoClearedTitle"), description: tr("customVideoClearedDescription") });%0A+     } catch (error) {%0A+       const message = userSafeError(error, tr("customVideoResetFailedMessage"));%0A+       setVideoStatus("failed");%0A+       setVideoMessage(message);%0A+       toast({ title: tr("customVideoResetFailedTitle"), description: message });%0A+     } finally {%0A+       setIsSavingVideo(false);%0A+     }%0A+   }%0A+%0A+   async function toggleFavorite() {%0A+     const currentWorkout = workout;%0A+     if (!currentWorkout || isFavoritePending) return;%0A+     const previousIds = favoriteIds;%0A+     const nextFavorite = !favorite;%0A+     const optimistic = nextFavorite%0A+       ? Array.from(new Set([...favoriteIds, currentWorkout.id]))%0A+       : favoriteIds.filter((id) => id !== currentWorkout.id);%0A+     setIsFavoritePending(true);%0A+     setFavoriteError("");%0A+     setFavoriteIds(optimistic);%0A+     try {%0A+       const next = await setFavoriteExercise(userId, currentWorkout.id, nextFavorite);%0A+       setFavoriteIds(next);%0A+       toast({ title: nextFavorite ? tr("exerciseFavorited") : tr("exerciseUnfavorited"), description: currentWorkout.name });%0A+     } catch (error) {%0A+       const message = userSafeError(error, tr("favoriteRestoreMessage"));%0A+       setFavoriteIds(previousIds);%0A+       setFavoriteError(message);%0A+       toast({ title: tr("favoriteNotSaved"), description: message });%0A+     } finally {%0A+       setIsFavoritePending(false);%0A+     }%0A+   }%0A+%0A+   return (%0A+     <TrainPageContainer className="space-y-6" dir={dir}>%0A+       <PageHeading%0A+         title={workout.name}%0A+         description={metadataLine(displayTarget, displayEquipment, displayDifficulty)}%0A+         action={%0A+           <div className="flex flex-wrap gap-2">%0A+             <Button className="min-h-12" variant={favorite ? "default" : "outline"} onClick={toggleFavorite} disabled={isFavoritePending} aria-busy={isFavoritePending}>%0A+               <Heart className={cn("h-4 w-4", favorite && "fill-current")} />%0A+               {isFavoritePending ? tr("saving") : favorite ? tr("savedLabel") : tr("favorite")}%0A+             </Button>%0A+             <Button asChild className="min-h-12">%0A+               <Link href={`/workouts/session/${workout.id}`}><Play className="h-4 w-4" /> {tr("startSession")}</Link>%0A+             </Button>%0A+           </div>%0A+         }%0A+       />%0A+       {loadWarning ? <InlineStatus tone="warning" title={tr("degradedExerciseData")} description={loadWarning} /> : null}%0A+       {favoriteError ? <InlineStatus tone="error" title={tr("favoriteNotSaved")} description={favoriteError} /> : null}%0A+       <div className="grid gap-4 lg:grid-cols-[0.9fr_1.1fr]">%0A+         <div className="space-y-4">%0A+           <Card>%0A+             <CardHeader><CardTitle>{tr("details")}</CardTitle></CardHeader>%0A+             <CardContent className="space-y-5">%0A+               {!workout.is_global ? <div><Badge variant="success">{tr("custom")}</Badge></div> : null}%0A+%0A+               <div className="grid gap-3 sm:grid-cols-2">%0A+                 <Detail label={tr("primaryMuscle")} value={displayTarget || tr("noneSaved")} />%0A+                 <Detail label={tr("secondaryMuscle")} value={displaySecondary || tr("noneSaved")} />%0A+                 <Detail label={tr("equipment")} value={displayEquipment || tr("noneSaved")} />%0A+                 <Detail label={tr("mechanics")} value={displayMovement || tr("noneSaved")} />%0A+                 <Detail label={tr("forceType")} value={displayForce || tr("noneSaved")} />%0A+                 <Detail label={tr("difficulty")} value={displayDifficulty || tr("noneSaved")} />%0A+               </div>%0A+%0A+               <TextPanel title={tr("instructions")} text={video?.instructions || workout.instructions} fallback={tr("noneSaved")} />%0A+               <TextPanel title={tr("mistakesToAvoid")} text={mistakes} fallback={tr("noneSaved")} />%0A+%0A+               <div className="grid gap-2 sm:grid-cols-2">%0A+                 {guideUrl ? <Button asChild variant="outline" className="min-h-12"><a href={guideUrl} target="_blank" rel="noreferrer"><ExternalLink className="h-4 w-4" /> {tr("openExerciseGuide")}</a></Button> : <Button type="button" variant="outline" className="min-h-12" disabled>{tr("noGuide")}</Button>}%0A+                 {customVideoUrl ? <Button asChild variant="outline" className="min-h-12"><a href={customVideoUrl} target="_blank" rel="noreferrer"><ExternalLink className="h-4 w-4" /> {tr("openCustomVideo")}</a></Button> : <Button type="button" variant="outline" className="min-h-12" disabled>{tr("noCustomVideo")}</Button>}%0A+               </div>%0A+%0A+               <div className="rounded-md border p-3">%0A+                 <Label htmlFor="custom-video-url">{tr("customVideoUrl")}</Label>%0A+                 <div className="mt-2 grid gap-2 sm:grid-cols-[1fr_auto_auto]">%0A+                   <Input%0A+                     id="custom-video-url"%0A+                     value={customVideoDraft}%0A+                     onChange={(event) => {%0A+                       setCustomVideoDraft(event.target.value);%0A+                       setVideoStatus("idle");%0A+                       setVideoMessage("");%0A+                     }}%0A+                     placeholder="https://youtube.com/watch?v=..."%0A+                     className="h-12"%0A+                     aria-invalid={customVideoInvalid}%0A+                   />%0A+                   <Button type="button" className="min-h-12" onClick={saveCustomVideo} disabled={isSavingVideo || !customVideoDraft.trim() || customVideoInvalid}><Save className="h-4 w-4" /> {isSavingVideo && videoStatus === "saving" ? tr("saving") : tr("save")}</Button>%0A+                   <Button type="button" variant="outline" className="min-h-12" onClick={resetCustomVideo} disabled={isSavingVideo || !customVideoUrl}><RotateCcw className="h-4 w-4" /> {tr("reset")}</Button>%0A+                 </div>%0A+                 {customVideoInvalid ? <p className="mt-2 text-sm leading-6 text-destructive">{tr("invalidCustomVideoUrl")}</p> : null}%0A+                 {videoMessage ? <InlineStatus className="mt-3" tone={videoStatus === "failed" ? "error" : "success"} title={videoStatus === "failed" ? tr("customVideoSaveFailed") : videoStatus === "reset" ? tr("customVideoClearedTitle") : tr("customVideoSavedTitle")} description={videoMessage} /> : null}%0A+               </div>%0A+             </CardContent>%0A+           </Card>%0A+%0A+           <Card>%0A+             <CardHeader><CardTitle className="flex items-center gap-2"><TrendingUp className="h-5 w-5 text-primary" /> {tr("realWorkoutHistory")}</CardTitle></CardHeader>%0A+             <CardContent className="space-y-4">%0A+               {!exerciseHistory?.count ? <p className="text-sm text-muted-foreground">{tr("noWorkoutHistory")}</p> : null}%0A+               {exerciseHistory?.count ? (%0A+                 <>%0A+                   <div className="grid gap-3 sm:grid-cols-3">%0A+                     <Detail label={tr("bestWeight")} value={`${exerciseHistory.bestWeight} kg`} />%0A+                     <Detail label={tr("bestReps")} value={`${exerciseHistory.bestReps}`} />%0A+                     <Detail label={tr("estimatedOneRepMax")} value={`${exerciseHistory.bestOneRepMax} kg`} />%0A+                   </div>%0A+                   <Detail label={tr("bestSet")} value={exerciseHistory.bestSet ? `${exerciseHistory.bestSet.weight_kg ?? 0} kg × ${exerciseHistory.bestSet.reps ?? 0}` : tr("noSetData")} />%0A+                   {exerciseHistory.trends.length < 2 ? <p className="text-sm text-muted-foreground">{tr("notEnoughHistory")}</p> : <div className="space-y-2">{exerciseHistory.trends.slice(-6).map((point) => <div key={point.date} className="rounded-md border p-3 text-sm"><p className="font-semibold">{point.date}</p><p className="text-muted-foreground">{point.volume} kg · {point.bestSetVolume} kg · 1RM {point.estimatedOneRepMax} kg</p></div>)}</div>}%0A+                 </>%0A+               ) : null}%0A+             </CardContent>%0A+           </Card>%0A+%0A+           <Card>%0A+             <CardHeader><CardTitle>{tr("alternativeExercises")}</CardTitle></CardHeader>%0A+             <CardContent className="grid gap-3 sm:grid-cols-2">%0A+               {!alternatives.length ? <p className="text-sm text-muted-foreground">{tr("noAlternatives")}</p> : null}%0A+               {alternatives.map((item) => {%0A+                 const metadata = metadataLine(formatExerciseDisplayList(item.target_muscle, language, "muscle"), formatExerciseDisplayList(item.equipment, language, "equipment"));%0A+                 return <Link key={item.id} href={`/workouts/${item.id}`} className="min-h-12 rounded-md border p-3 transition hover:border-primary"><p className="font-semibold">{item.name}</p>{metadata ? <p className="text-sm text-muted-foreground">{metadata}</p> : null}</Link>;%0A+               })}%0A+             </CardContent>%0A+           </Card>%0A+         </div>%0A+         <ExerciseVideoPlayer video={displayVideo} />%0A+       </div>%0A+     </TrainPageContainer>%0A+   );%0A+ }%0A+%0A+ function TextPanel({ title, text, fallback }: { title: string; text: string | null | undefined; fallback: string }) {%0A+   return (%0A+     <details className="group rounded-md bg-muted/40">%0A+       <summary className="flex min-h-12 cursor-pointer list-none items-center justify-between gap-2 p-3 text-sm font-semibold text-foreground">%0A+         {title}%0A+         <ChevronDown className="h-4 w-4 text-muted-foreground transition group-open:rotate-180" />%0A+       </summary>%0A+       <p className="px-3 pb-3 leading-7 text-muted-foreground">{text || fallback}</p>%0A+     </details>%0A+   );%0A+ }%0A+%0A+ function Detail({ label, value }: { label: string | null | undefined; value: string | null | undefined }) {%0A+   return <div className="rounded-md bg-muted/40 p-3"><p className="text-xs font-semibold uppercase tracking-normal text-muted-foreground">{label}</p><p className="mt-1 text-sm font-medium text-foreground">{value || "N/A"}</p></div>;%0A+ }%0A+%0A+ function InlineStatus({%0A+   tone,%0A+   title,%0A+   description,%0A+   className%0A+ }: {%0A+   tone: "success" | "warning" | "error";%0A+   title: string;%0A+   description: string;%0A+   className?: string;%0A+ }) {%0A+   const Icon = tone === "success" ? CheckCircle2 : AlertTriangle;%0A+   const toneClass = tone === "error"%0A+     ? "border-destructive/30 bg-destructive/5"%0A+     : tone === "warning"%0A+       ? "border-warning/30 bg-warning/10"%0A+       : "border-success/30 bg-success/5";%0A+   const iconClass = tone === "error"%0A+     ? "text-destructive"%0A+     : tone === "warning"%0A+       ? "text-warning"%0A+       : "text-success";%0A+%0A+   return (%0A+     <Card className={cn(toneClass, className)}>%0A+       <CardContent className="flex items-start gap-3 p-4">%0A+         <Icon className={cn("mt-0.5 h-5 w-5 shrink-0", iconClass)} />%0A+         <div>%0A+           <p className="font-semibold text-foreground">{title}</p>%0A+           <p className="mt-1 text-sm leading-6 text-muted-foreground">{description}</p>%0A+         </div>%0A+       </CardContent>%0A+     </Card>%0A+   );%0A+ }%0A+%0A%0A ❯ lib/product/train-phase1-approved-ui.test.ts:86:20%0A%0A

::error file=/tmp/aw1a-main/lib/product/train-phase2a-architecture.test.ts,title=lib/product/train-phase2a-architecture.test.ts > Train Phase 2A architecture contract > enforces privacy%2C ownership%2C JSON shape%2C and verification in the authoritative quality gate,line=96,column=22::AssertionError: expected 'name: Quality\n\non:\n  pull_request:…' to contain '-f supabase/verification/train-phase2…'%0A%0A- Expected%0A+ Received%0A%0A- -f supabase/verification/train-phase2a-program-architecture.sql%0A+ name: Quality%0A+%0A+ on:%0A+   pull_request:%0A+     branches:%0A+       - main%0A+   workflow_dispatch:%0A+%0A+ permissions:%0A+   contents: read%0A+%0A+ concurrency:%0A+   group: quality-${{ github.workflow }}-${{ github.ref }}%0A+   cancel-in-progress: true%0A+%0A+ jobs:%0A+   verify:%0A+     runs-on: ubuntu-latest%0A+     timeout-minutes: 30%0A+     env:%0A+       NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co%0A+       NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-public-key-long-enough%0A+       SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-service-role-key-long-enough%0A+       NEXT_PUBLIC_APP_URL: http://localhost:3000%0A+       NEXT_PUBLIC_USE_MOCK_AUTH: "true"%0A+       PLAIVRA_MCP_BASE_URL: http://localhost:3000/api/mcp%0A+       PLAIVRA_MCP_TOKEN_SECRET: ci-placeholder-token-secret-that-is-long-enough%0A+       PLAIVRA_CHATGPT_REDIRECT_URIS: https://chatgpt.com/connector_platform_oauth_redirect%0A+       PLAIVRA_CIMD_ALLOWED_ORIGINS: https://chatgpt.com%0A+       PLAIVRA_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}%0A+       PLAIVRA_RELEASE_ENVIRONMENT: ci%0A+       PLAIVRA_SCHEMA_COMPATIBILITY_VERSION: "2"%0A+%0A+     steps:%0A+       - name: Checkout exact revision%0A+         uses: actions/checkout@v4%0A+         with:%0A+           ref: ${{ github.event.pull_request.head.sha || github.sha }}%0A+           fetch-depth: 0%0A+%0A+       - name: Verify checkout identity%0A+         shell: bash%0A+         run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha || github.sha }}"%0A+%0A+       - name: Detect relevant verification scope%0A+         id: scope%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             changed_files="$(git ls-files)"%0A+           else%0A+             base_sha="${{ github.event.pull_request.base.sha }}"%0A+             changed_files="$(git diff --name-only "${base_sha}...HEAD")"%0A+           fi%0A+%0A+           printf '%25s\n' "$changed_files"%0A+%0A+           code=false%0A+           runtime=false%0A+           dependencies=false%0A+           database=false%0A+           integration=false%0A+           scripts=false%0A+           environment=false%0A+           ui=false%0A+           train_ui=false%0A+%0A+           if grep -Eq '\.(ts|tsx|js|jsx|mjs|cjs)$|^(package(-lock)?\.json|tsconfig\.json|vitest\..*\.mjs|eslint\.config\.)' <<< "$changed_files"; then%0A+             code=true%0A+           fi%0A+%0A+           if grep -Eq '^(app/|components/|lib/|services/|types/|middleware\.|next\.config\.|package\.json)' <<< "$changed_files" \%0A+             && grep -Eqv '(\.test\.|\.spec\.|^docs/)' <<< "$changed_files"; then%0A+             runtime=true%0A+           fi%0A+%0A+           if grep -Eq '^package-lock\.json$' <<< "$changed_files"; then dependencies=true; fi%0A+           if grep -Eq '^(supabase/migrations/|supabase/migration-ledger\.json|scripts/(check-migration-ledger|test-database-preflight-control)|services/database/|types/database\.ts)' <<< "$changed_files"; then database=true; fi%0A+           if grep -Eq '^(services/database/|app/api/|supabase/migrations/|vitest\.integration\.config\.mjs)' <<< "$changed_files"; then integration=true; fi%0A+           if grep -Eq '^scripts/' <<< "$changed_files"; then scripts=true; fi%0A+           if grep -Eq '^(scripts/validate-production-env\.mjs|lib/integrations/env|next\.config\.|vercel\.json|package-lock\.json)' <<< "$changed_files"; then environment=true; fi%0A+           if grep -Eq '^(app/|components/|public/|styles/|lib/i18n/|next\.config\.|postcss\.config\.|tailwind\.config\.)' <<< "$changed_files"; then ui=true; fi%0A+           if grep -Eq '^(app/.*/train/|components/train/|components/workouts/|lib/train/|services/activity-catalog/|app/api/activity-catalog/)' <<< "$changed_files"; then train_ui=true; fi%0A+%0A+           {%0A+             echo "code=${code}"%0A+             echo "runtime=${runtime}"%0A+             echo "dependencies=${dependencies}"%0A+             echo "database=${database}"%0A+             echo "integration=${integration}"%0A+             echo "scripts=${scripts}"%0A+             echo "environment=${environment}"%0A+             echo "ui=${ui}"%0A+             echo "train_ui=${train_ui}"%0A+           } >> "$GITHUB_OUTPUT"%0A+%0A+       - name: Set up Node.js%0A+         if: steps.scope.outputs.code == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         uses: actions/setup-node@v4%0A+         with:%0A+           node-version: 24%0A+           cache: npm%0A+%0A+       - name: Install dependencies%0A+         if: steps.scope.outputs.code == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         run: npm ci%0A+%0A+       - name: Lint changed source files%0A+         if: steps.scope.outputs.code == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             mapfile -d '' files < <(git ls-files -z | grep -zE '\.(ts|tsx|js|jsx|mjs|cjs)$' || true)%0A+           else%0A+             base_sha="${{ github.event.pull_request.base.sha }}"%0A+             mapfile -d '' files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" | grep -zE '\.(ts|tsx|js|jsx|mjs|cjs)$' || true)%0A+           fi%0A+           if [ "${#files[@]}" -gt 0 ]; then%0A+             npx eslint -- "${files[@]}"%0A+           fi%0A+%0A+       - name: Typecheck application%0A+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         run: npm run typecheck%0A+%0A+       - name: Run tests related to changed code%0A+         if: steps.scope.outputs.code == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             npm run test:unit%0A+             exit 0%0A+           fi%0A+%0A+           base_sha="${{ github.event.pull_request.base.sha }}"%0A+           mapfile -d '' test_files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" | grep -zE '\.(test|spec)\.(ts|tsx|js|jsx)$' || true)%0A+           if [ "${#test_files[@]}" -gt 0 ]; then%0A+             npx vitest run --config vitest.unit.config.mjs "${test_files[@]}"%0A+           fi%0A+%0A+           mapfile -d '' source_files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" \%0A+             | grep -zE '\.(ts|tsx|js|jsx)$' \%0A+             | grep -zEv '\.(test|spec)\.(ts|tsx|js|jsx)$' || true)%0A+           if [ "${#source_files[@]}" -gt 0 ]; then%0A+             npx vitest related "${source_files[@]}" --run --config vitest.unit.config.mjs --passWithNoTests%0A+           fi%0A+%0A+       - name: Check changed production dependencies%0A+         if: steps.scope.outputs.dependencies == 'true'%0A+         run: npm audit --omit=dev --audit-level=moderate%0A+%0A+       - name: Run changed script tests%0A+         if: steps.scope.outputs.scripts == 'true'%0A+         run: npm run test:scripts%0A+%0A+       - name: Run integration tests for API or database changes%0A+         if: steps.scope.outputs.integration == 'true'%0A+         run: npm run test:integration%0A+%0A+       - name: Set up Supabase CLI%0A+         if: steps.scope.outputs.database == 'true'%0A+         uses: supabase/setup-cli@v2%0A+         with:%0A+           version: 2.109.1%0A+           github-token: ${{ github.token }}%0A+%0A+       - name: Replay and lint database migrations%0A+         if: steps.scope.outputs.database == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           supabase db start%0A+           supabase db reset --local --no-seed%0A+           supabase db lint --local --schema public --level error --fail-on error%0A+           npm run migration:ledger:check%0A+%0A+       - name: Verify production database security contracts%0A+         if: steps.scope.outputs.database == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           database_url="postgresql://postgres:postgres@127.0.0.1:54322/postgres"%0A+           PGPASSWORD=postgres psql "$database_url" -X -v ON_ERROR_STOP=1 -f supabase/verification/train-atomic-rpc-security.sql%0A+           PLAIVRA_PREFLIGHT_TEST_DATABASE_URL="$database_url" node scripts/test-database-preflight-control.mjs%0A+           PGPASSWORD=postgres psql "$database_url" -X -v ON_ERROR_STOP=1 -f supabase/verification/production-release-migration-preflight.sql%0A+%0A+       - name: Validate production environment contract%0A+         if: steps.scope.outputs.environment == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           export PLAIVRA_VALIDATE_PRODUCTION_ENV=true%0A+           export PLAIVRA_RELEASE_ENVIRONMENT=production%0A+           export NEXT_PUBLIC_APP_URL=https://app.plaivra.com%0A+           export NEXT_PUBLIC_USE_MOCK_AUTH=false%0A+           export PLAIVRA_MCP_BASE_URL=https://app.plaivra.com/api/mcp%0A+           export PLAIVRA_OAUTH_ISSUER=https://app.plaivra.com%0A+           export PLAIVRA_CIMD_ALLOWED_ORIGINS=https://chatgpt.com%0A+           export CRON_SECRET=ci-placeholder-cron-secret-that-is-long-enough%0A+           npm run validate:env%0A+%0A+       - name: Build production application%0A+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           export PLAIVRA_BUILD_TIMESTAMP="$(date -u +'%25Y-%25m-%25dT%25H:%25M:%25SZ')"%0A+           NEXT_PUBLIC_USE_MOCK_AUTH=false npm run build%0A+%0A+       - name: Install Playwright Chromium%0A+         if: steps.scope.outputs.ui == 'true'%0A+         run: npx playwright install --with-deps chromium%0A+%0A+       - name: Run relevant browser QA%0A+         if: steps.scope.outputs.ui == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           mkdir -p quality-reports%0A+           NEXT_PUBLIC_USE_MOCK_AUTH=true npm run dev > quality-reports/qa-server.log 2>&1 &%0A+           server_pid=$!%0A+           trap 'kill "$server_pid" 2>/dev/null || true' EXIT%0A+           for attempt in $(seq 1 60); do%0A+             if curl --fail --silent http://localhost:3000/ > /dev/null; then break; fi%0A+             sleep 1%0A+           done%0A+           curl --fail --silent http://localhost:3000/ > /dev/null%0A+%0A+           if [ "${{ steps.scope.outputs.train_ui }}" = "true" ]; then%0A+             QA_BASE_URL=http://localhost:3000 QA_TRAIN_EVIDENCE_DIR=quality-reports/train-qa-evidence npm run qa:train%0A+           else%0A+             QA_BASE_URL=http://localhost:3000 QA_EVIDENCE_DIR=quality-reports/rendered-qa-evidence npm run qa:rendered%0A+           fi%0A+%0A+       - name: Upload browser evidence on failure%0A+         if: failure() && steps.scope.outputs.ui == 'true'%0A+         uses: actions/upload-artifact@v4%0A+         with:%0A+           name: quality-failure-evidence-${{ github.run_id }}%0A+           path: quality-reports/%0A+           if-no-files-found: ignore%0A+           retention-days: 7%0A+%0A%0A ❯ lib/product/train-phase2a-architecture.test.ts:96:22%0A%0A
```

## npm-audit.log — final output
```text
found 0 vulnerabilities
```

## npm-ci.log — final output
```text

added 618 packages, and audited 619 packages in 16s

187 packages are looking for funding
  run `npm fund` for details

found 0 vulnerabilities
npm warn allow-scripts 4 packages have install scripts not yet covered by allowScripts:
npm warn allow-scripts   @parcel/watcher@2.6.0 (install: node scripts/build-from-source.js)
npm warn allow-scripts   @swc/core@1.15.46 (postinstall: node postinstall.js)
npm warn allow-scripts   sharp@0.34.5 (install: node install/check.js || npm run build)
npm warn allow-scripts   unrs-resolver@1.12.2 (postinstall: node postinstall.js)
npm warn allow-scripts
npm warn allow-scripts Run `npm approve-scripts --allow-scripts-pending` to review, or `npm approve-scripts <pkg>` to allow.
```

## production-server.log — final output
```text

> plaivra@1.0.0 start
> next start

▲ Next.js 16.2.10
- Local:         http://localhost:3000
- Network:       http://10.1.0.194:3000
✓ Ready in 119ms
```

## raw-qa.log — final output
```text
AW-1A raw server QA passed: 5 cases.
```

## script-tests.log — final output
```text
✔ normalizes duplicates and case without weakening exactness (0.153435ms)
✔ rendered QA preserves the 126-route matrix and isolates every observation (1.35215ms)
✔ today-workout is redirected before the React route lifecycle (0.405718ms)
✔ authenticated evidence is fail-closed and always emits a safe failure summary (0.307813ms)
✔ post-deploy readiness gates are production-only while artifact identity stays universal (1.764837ms)
✔ repository config declares Vercel main-only policy intent without claiming provider enforcement (3.800694ms)
✔ release documentation distinguishes repository intent from actual provider verification (0.517272ms)
✔ launch documentation places reconciliation, strict preflight, and owner approval before merge (0.680401ms)
✔ active configuration has no Vercel preview or production SHA approval dependency (0.619452ms)
✔ tracked references contain no active obsolete Vercel gate dependency (92.839435ms)
✖ git diff checks pass for the working tree and branch range (123.361264ms)
✔ Netlify keeps its exact-SHA ignore command and local behavior (32.715773ms)
✔ Netlify deploy-preview behavior remains unchanged (36.225084ms)
✔ Netlify branch-deploy behavior remains unchanged (32.585722ms)
✔ Netlify dev behavior remains unchanged (70.874578ms)
✔ Netlify production is held without exact approval (37.003719ms)
✔ Netlify production is held for a mismatched approval (30.6785ms)
✔ Netlify production is held for abbreviated or malformed identities (118.553609ms)
✔ Netlify production proceeds only for the exact valid 40-character SHA (31.518547ms)
✔ Netlify ambiguous provider targets fail closed (29.088115ms)
✔ next.config bundles deterministic release and migration metadata (57.738621ms)
✔ post-deploy smoke rejects abbreviated release SHAs before network access (57.539292ms)
✔ release manifest rejects abbreviated release SHAs (75.075921ms)
✔ release manifest records exact installed runtime versions and lockfile identity (328.043461ms)
✔ built metadata verification accepts reconciled ledger metadata while runtime readiness remains fail-closed (0.799547ms)
✔ Netlify production release gate rejects nonstandard SHA lengths (36.306854ms)
✔ defaults programmatic evaluation to strict release mode (2.7792ms)
✔ defaults omitted mode to release even in a pull-request environment (0.437794ms)
✔ strict release mode rejects a valid pending-only migration state (0.529329ms)
✔ default release mode rejects a valid pending-only migration state (0.317887ms)
✔ review mode accepts a valid pending-only migration state without claiming release readiness (0.296456ms)
✔ review mode rejects schema-applied-untracked migration state (0.38721ms)
✔ review mode rejects unresolved counts that differ from pending counts (0.296857ms)
✔ both modes reject an old deployment artifact as a substitute for the reviewed SHA (0.555518ms)
✔ both modes reject manifest count drift (0.470443ms)
✔ review mode still distinguishes missing and failed quality evidence (0.491353ms)
✔ review mode still rejects commit-mismatched and stale quality evidence (0.261615ms)
✔ reconciled state passes both review and release modes (0.228697ms)
✔ rejects a manifest whose Next.js evidence differs from the installed runtime (1.0738ms)
✔ unknown modes fail closed (0.659611ms)
✔ does not enforce provider configuration for ordinary local builds (2.114983ms)
✔ accepts complete core production configuration (0.374231ms)
✔ rejects missing and malformed core production configuration without values (0.603869ms)
✔ validates MCP and OAuth configuration only when enabled (0.337988ms)
✔ validates Stripe configuration only when checkout is enabled (0.304929ms)
✔ validates Activity Catalog configuration only for external modes (0.559604ms)
✔ validates privacy execution key and retention periods conditionally (0.513535ms)
ℹ tests 73
ℹ suites 0
ℹ pass 72
ℹ fail 1
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 873.520315

✖ failing tests:

test at scripts/provider-release-gates.test.mjs:125:1
✖ git diff checks pass for the working tree and branch range (123.361264ms)
  AssertionError [ERR_ASSERTION]: aw1a-validation-diagnostics.md:381: new blank line at EOF.
  
  
  
  2 !== 0
  
      at TestContext.<anonymous> (file:///home/runner/work/gymsands/gymsands/scripts/provider-release-gates.test.mjs:132:10)
      at Test.runInAsyncScope (node:async_hooks:227:14)
      at Test.run (node:internal/test_runner/test:1325:25)
      at Test.processPendingSubtests (node:internal/test_runner/test:911:18)
      at Test.postRun (node:internal/test_runner/test:1465:19)
      at Test.run (node:internal/test_runner/test:1390:12)
      at async Test.processPendingSubtests (node:internal/test_runner/test:911:7) {
    generatedMessage: false,
    code: 'ERR_ASSERTION',
    actual: 2,
    expected: 0,
    operator: 'strictEqual',
    diff: 'simple'
  }
```

## targeted-tests.log — final output
```text

[1m[30m[46m RUN [49m[39m[22m [36mv4.1.9 [39m[90m/home/runner/work/gymsands/gymsands[39m

 [32m✓[39m lib/i18n/message-shape.test.ts [2m([22m[2m2 tests[22m[2m)[22m[32m 5[2mms[22m[39m
 [32m✓[39m lib/i18n/locale-resolution.test.ts [2m([22m[2m37 tests[22m[2m)[22m[32m 11[2mms[22m[39m

[2m Test Files [22m [1m[32m2 passed[39m[22m[90m (2)[39m
[2m      Tests [22m [1m[32m39 passed[39m[22m[90m (39)[39m
[2m   Start at [22m 07:45:15
[2m   Duration [22m 690ms[2m (transform 230ms, setup 0ms, import 438ms, tests 16ms, environment 0ms)[22m

```

## typecheck.log — final output
```text

> plaivra@1.0.0 typecheck
> tsc --noEmit

```

## unit-tests.log — final output
```text
[31m+         shell: bash[39m
[31m+         run: |[39m
[31m+           set -euo pipefail[39m
[31m+           export PLAIVRA_VALIDATE_PRODUCTION_ENV=true[39m
[31m+           export PLAIVRA_RELEASE_ENVIRONMENT=production[39m
[31m+           export NEXT_PUBLIC_APP_URL=https://app.plaivra.com[39m
[31m+           export NEXT_PUBLIC_USE_MOCK_AUTH=false[39m
[31m+           export PLAIVRA_MCP_BASE_URL=https://app.plaivra.com/api/mcp[39m
[31m+           export PLAIVRA_OAUTH_ISSUER=https://app.plaivra.com[39m
[31m+           export PLAIVRA_CIMD_ALLOWED_ORIGINS=https://chatgpt.com[39m
[31m+           export CRON_SECRET=ci-placeholder-cron-secret-that-is-long-enough[39m
[31m+           npm run validate:env[39m
[31m+[39m
[31m+       - name: Build production application[39m
[31m+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'[39m
[31m+         shell: bash[39m
[31m+         run: |[39m
[31m+           set -euo pipefail[39m
[31m+           export PLAIVRA_BUILD_TIMESTAMP="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"[39m
[31m+           NEXT_PUBLIC_USE_MOCK_AUTH=false npm run build[39m
[31m+[39m
[31m+       - name: Install Playwright Chromium[39m
[31m+         if: steps.scope.outputs.ui == 'true'[39m
[31m+         run: npx playwright install --with-deps chromium[39m
[31m+[39m
[31m+       - name: Run relevant browser QA[39m
[31m+         if: steps.scope.outputs.ui == 'true'[39m
[31m+         shell: bash[39m
[31m+         run: |[39m
[31m+           set -euo pipefail[39m
[31m+           mkdir -p quality-reports[39m
[31m+           NEXT_PUBLIC_USE_MOCK_AUTH=true npm run dev > quality-reports/qa-server.log 2>&1 &[39m
[31m+           server_pid=$![39m
[31m+           trap 'kill "$server_pid" 2>/dev/null || true' EXIT[39m
[31m+           for attempt in $(seq 1 60); do[39m
[31m+             if curl --fail --silent http://localhost:3000/ > /dev/null; then break; fi[39m
[31m+             sleep 1[39m
[31m+           done[39m
[31m+           curl --fail --silent http://localhost:3000/ > /dev/null[39m
[31m+[39m
[31m+           if [ "${{ steps.scope.outputs.train_ui }}" = "true" ]; then[39m
[31m+             QA_BASE_URL=http://localhost:3000 QA_TRAIN_EVIDENCE_DIR=quality-reports/train-qa-evidence npm run qa:train[39m
[31m+           else[39m
[31m+             QA_BASE_URL=http://localhost:3000 QA_EVIDENCE_DIR=quality-reports/rendered-qa-evidence npm run qa:rendered[39m
[31m+           fi[39m
[31m+[39m
[31m+       - name: Upload browser evidence on failure[39m
[31m+         if: failure() && steps.scope.outputs.ui == 'true'[39m
[31m+         uses: actions/upload-artifact@v4[39m
[31m+         with:[39m
[31m+           name: quality-failure-evidence-${{ github.run_id }}[39m
[31m+           path: quality-reports/[39m
[31m+           if-no-files-found: ignore[39m
[31m+           retention-days: 7[39m
[31m+[39m

[36m [2m❯[22m lib/product/train-phase2a-architecture.test.ts:[2m96:22[22m[39m
    [90m 94|[39m     expect(verification).toContain("failed detach left a partial clone…
    [90m 95|[39m     expect(verification).toContain("account deletion did not remove tr…
    [90m 96|[39m     expect(workflow).toContain("-f supabase/verification/train-phase2a…
    [90m   |[39m                      [31m^[39m
    [90m 97|[39m   })[33m;[39m
    [90m 98|[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/4]⎯[22m[39m


[2m Test Files [22m [1m[31m3 failed[39m[22m[2m | [22m[1m[32m176 passed[39m[22m[90m (179)[39m
[2m      Tests [22m [1m[31m4 failed[39m[22m[2m | [22m[1m[32m1174 passed[39m[22m[90m (1178)[39m
[2m   Start at [22m 07:45:16
[2m   Duration [22m 17.55s[2m (transform 4.33s, setup 0ms, import 13.53s, tests 5.93s, environment 2.43s)[22m


::error file=/home/runner/work/gymsands/gymsands/lib/product/muscle-intelligence-phase1-migration.test.ts,title=lib/product/muscle-intelligence-phase1-migration.test.ts > Muscle Intelligence Phase 1 migration contract > executes the disposable Phase 1 verification in the authoritative Quality database preflight,line=101,column=29::AssertionError: expected 'name: Quality\n\non:\n  pull_request:…' to contain 'PGPASSWORD=postgres psql "postgresql:…'%0A%0A- Expected%0A+ Received%0A%0A- PGPASSWORD=postgres psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -X -v ON_ERROR_STOP=1 -f supabase/verification/muscle-intelligence-phase1.sql%0A+ name: Quality%0A+%0A+ on:%0A+   pull_request:%0A+     branches:%0A+       - main%0A+   workflow_dispatch:%0A+%0A+ permissions:%0A+   contents: read%0A+%0A+ concurrency:%0A+   group: quality-${{ github.workflow }}-${{ github.ref }}%0A+   cancel-in-progress: true%0A+%0A+ jobs:%0A+   verify:%0A+     runs-on: ubuntu-latest%0A+     timeout-minutes: 30%0A+     env:%0A+       NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co%0A+       NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-public-key-long-enough%0A+       SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-service-role-key-long-enough%0A+       NEXT_PUBLIC_APP_URL: http://localhost:3000%0A+       NEXT_PUBLIC_USE_MOCK_AUTH: "true"%0A+       PLAIVRA_MCP_BASE_URL: http://localhost:3000/api/mcp%0A+       PLAIVRA_MCP_TOKEN_SECRET: ci-placeholder-token-secret-that-is-long-enough%0A+       PLAIVRA_CHATGPT_REDIRECT_URIS: https://chatgpt.com/connector_platform_oauth_redirect%0A+       PLAIVRA_CIMD_ALLOWED_ORIGINS: https://chatgpt.com%0A+       PLAIVRA_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}%0A+       PLAIVRA_RELEASE_ENVIRONMENT: ci%0A+       PLAIVRA_SCHEMA_COMPATIBILITY_VERSION: "2"%0A+%0A+     steps:%0A+       - name: Checkout exact revision%0A+         uses: actions/checkout@v4%0A+         with:%0A+           ref: ${{ github.event.pull_request.head.sha || github.sha }}%0A+           fetch-depth: 0%0A+%0A+       - name: Verify checkout identity%0A+         shell: bash%0A+         run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha || github.sha }}"%0A+%0A+       - name: Detect relevant verification scope%0A+         id: scope%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             changed_files="$(git ls-files)"%0A+           else%0A+             base_sha="${{ github.event.pull_request.base.sha }}"%0A+             changed_files="$(git diff --name-only "${base_sha}...HEAD")"%0A+           fi%0A+%0A+           printf '%25s\n' "$changed_files"%0A+%0A+           code=false%0A+           runtime=false%0A+           dependencies=false%0A+           database=false%0A+           integration=false%0A+           scripts=false%0A+           environment=false%0A+           ui=false%0A+           train_ui=false%0A+%0A+           if grep -Eq '\.(ts|tsx|js|jsx|mjs|cjs)$|^(package(-lock)?\.json|tsconfig\.json|vitest\..*\.mjs|eslint\.config\.)' <<< "$changed_files"; then%0A+             code=true%0A+           fi%0A+%0A+           if grep -Eq '^(app/|components/|lib/|services/|types/|middleware\.|next\.config\.|package\.json)' <<< "$changed_files" \%0A+             && grep -Eqv '(\.test\.|\.spec\.|^docs/)' <<< "$changed_files"; then%0A+             runtime=true%0A+           fi%0A+%0A+           if grep -Eq '^package-lock\.json$' <<< "$changed_files"; then dependencies=true; fi%0A+           if grep -Eq '^(supabase/migrations/|supabase/migration-ledger\.json|scripts/(check-migration-ledger|test-database-preflight-control)|services/database/|types/database\.ts)' <<< "$changed_files"; then database=true; fi%0A+           if grep -Eq '^(services/database/|app/api/|supabase/migrations/|vitest\.integration\.config\.mjs)' <<< "$changed_files"; then integration=true; fi%0A+           if grep -Eq '^scripts/' <<< "$changed_files"; then scripts=true; fi%0A+           if grep -Eq '^(scripts/validate-production-env\.mjs|lib/integrations/env|next\.config\.|vercel\.json|package-lock\.json)' <<< "$changed_files"; then environment=true; fi%0A+           if grep -Eq '^(app/|components/|public/|styles/|lib/i18n/|next\.config\.|postcss\.config\.|tailwind\.config\.)' <<< "$changed_files"; then ui=true; fi%0A+           if grep -Eq '^(app/.*/train/|components/train/|components/workouts/|lib/train/|services/activity-catalog/|app/api/activity-catalog/)' <<< "$changed_files"; then train_ui=true; fi%0A+%0A+           {%0A+             echo "code=${code}"%0A+             echo "runtime=${runtime}"%0A+             echo "dependencies=${dependencies}"%0A+             echo "database=${database}"%0A+             echo "integration=${integration}"%0A+             echo "scripts=${scripts}"%0A+             echo "environment=${environment}"%0A+             echo "ui=${ui}"%0A+             echo "train_ui=${train_ui}"%0A+           } >> "$GITHUB_OUTPUT"%0A+%0A+       - name: Set up Node.js%0A+         if: steps.scope.outputs.code == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         uses: actions/setup-node@v4%0A+         with:%0A+           node-version: 24%0A+           cache: npm%0A+%0A+       - name: Install dependencies%0A+         if: steps.scope.outputs.code == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         run: npm ci%0A+%0A+       - name: Lint changed source files%0A+         if: steps.scope.outputs.code == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             mapfile -d '' files < <(git ls-files -z | grep -zE '\.(ts|tsx|js|jsx|mjs|cjs)$' || true)%0A+           else%0A+             base_sha="${{ github.event.pull_request.base.sha }}"%0A+             mapfile -d '' files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" | grep -zE '\.(ts|tsx|js|jsx|mjs|cjs)$' || true)%0A+           fi%0A+           if [ "${#files[@]}" -gt 0 ]; then%0A+             npx eslint -- "${files[@]}"%0A+           fi%0A+%0A+       - name: Typecheck application%0A+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         run: npm run typecheck%0A+%0A+       - name: Run tests related to changed code%0A+         if: steps.scope.outputs.code == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             npm run test:unit%0A+             exit 0%0A+           fi%0A+%0A+           base_sha="${{ github.event.pull_request.base.sha }}"%0A+           mapfile -d '' test_files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" | grep -zE '\.(test|spec)\.(ts|tsx|js|jsx)$' || true)%0A+           if [ "${#test_files[@]}" -gt 0 ]; then%0A+             npx vitest run --config vitest.unit.config.mjs "${test_files[@]}"%0A+           fi%0A+%0A+           mapfile -d '' source_files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" \%0A+             | grep -zE '\.(ts|tsx|js|jsx)$' \%0A+             | grep -zEv '\.(test|spec)\.(ts|tsx|js|jsx)$' || true)%0A+           if [ "${#source_files[@]}" -gt 0 ]; then%0A+             npx vitest related "${source_files[@]}" --run --config vitest.unit.config.mjs --passWithNoTests%0A+           fi%0A+%0A+       - name: Check changed production dependencies%0A+         if: steps.scope.outputs.dependencies == 'true'%0A+         run: npm audit --omit=dev --audit-level=moderate%0A+%0A+       - name: Run changed script tests%0A+         if: steps.scope.outputs.scripts == 'true'%0A+         run: npm run test:scripts%0A+%0A+       - name: Run integration tests for API or database changes%0A+         if: steps.scope.outputs.integration == 'true'%0A+         run: npm run test:integration%0A+%0A+       - name: Set up Supabase CLI%0A+         if: steps.scope.outputs.database == 'true'%0A+         uses: supabase/setup-cli@v2%0A+         with:%0A+           version: 2.109.1%0A+           github-token: ${{ github.token }}%0A+%0A+       - name: Replay and lint database migrations%0A+         if: steps.scope.outputs.database == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           supabase db start%0A+           supabase db reset --local --no-seed%0A+           supabase db lint --local --schema public --level error --fail-on error%0A+           npm run migration:ledger:check%0A+%0A+       - name: Verify production database security contracts%0A+         if: steps.scope.outputs.database == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           database_url="postgresql://postgres:postgres@127.0.0.1:54322/postgres"%0A+           PGPASSWORD=postgres psql "$database_url" -X -v ON_ERROR_STOP=1 -f supabase/verification/train-atomic-rpc-security.sql%0A+           PLAIVRA_PREFLIGHT_TEST_DATABASE_URL="$database_url" node scripts/test-database-preflight-control.mjs%0A+           PGPASSWORD=postgres psql "$database_url" -X -v ON_ERROR_STOP=1 -f supabase/verification/production-release-migration-preflight.sql%0A+%0A+       - name: Validate production environment contract%0A+         if: steps.scope.outputs.environment == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           export PLAIVRA_VALIDATE_PRODUCTION_ENV=true%0A+           export PLAIVRA_RELEASE_ENVIRONMENT=production%0A+           export NEXT_PUBLIC_APP_URL=https://app.plaivra.com%0A+           export NEXT_PUBLIC_USE_MOCK_AUTH=false%0A+           export PLAIVRA_MCP_BASE_URL=https://app.plaivra.com/api/mcp%0A+           export PLAIVRA_OAUTH_ISSUER=https://app.plaivra.com%0A+           export PLAIVRA_CIMD_ALLOWED_ORIGINS=https://chatgpt.com%0A+           export CRON_SECRET=ci-placeholder-cron-secret-that-is-long-enough%0A+           npm run validate:env%0A+%0A+       - name: Build production application%0A+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           export PLAIVRA_BUILD_TIMESTAMP="$(date -u +'%25Y-%25m-%25dT%25H:%25M:%25SZ')"%0A+           NEXT_PUBLIC_USE_MOCK_AUTH=false npm run build%0A+%0A+       - name: Install Playwright Chromium%0A+         if: steps.scope.outputs.ui == 'true'%0A+         run: npx playwright install --with-deps chromium%0A+%0A+       - name: Run relevant browser QA%0A+         if: steps.scope.outputs.ui == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           mkdir -p quality-reports%0A+           NEXT_PUBLIC_USE_MOCK_AUTH=true npm run dev > quality-reports/qa-server.log 2>&1 &%0A+           server_pid=$!%0A+           trap 'kill "$server_pid" 2>/dev/null || true' EXIT%0A+           for attempt in $(seq 1 60); do%0A+             if curl --fail --silent http://localhost:3000/ > /dev/null; then break; fi%0A+             sleep 1%0A+           done%0A+           curl --fail --silent http://localhost:3000/ > /dev/null%0A+%0A+           if [ "${{ steps.scope.outputs.train_ui }}" = "true" ]; then%0A+             QA_BASE_URL=http://localhost:3000 QA_TRAIN_EVIDENCE_DIR=quality-reports/train-qa-evidence npm run qa:train%0A+           else%0A+             QA_BASE_URL=http://localhost:3000 QA_EVIDENCE_DIR=quality-reports/rendered-qa-evidence npm run qa:rendered%0A+           fi%0A+%0A+       - name: Upload browser evidence on failure%0A+         if: failure() && steps.scope.outputs.ui == 'true'%0A+         uses: actions/upload-artifact@v4%0A+         with:%0A+           name: quality-failure-evidence-${{ github.run_id }}%0A+           path: quality-reports/%0A+           if-no-files-found: ignore%0A+           retention-days: 7%0A+%0A%0A ❯ lib/product/muscle-intelligence-phase1-migration.test.ts:101:29%0A%0A

::error file=/home/runner/work/gymsands/gymsands/lib/product/train-phase1-approved-ui.test.ts,title=lib/product/train-phase1-approved-ui.test.ts > approved Train Phase 1 UI contracts > keeps picker selection%2C duplicates%2C keyboard selection%2C focus return%2C request grouping%2C cancellation%2C and explicit pagination,line=42,column=20::AssertionError: expected '"use client";\n\nimport { Check, Dumb…' to contain 'className="absolute inset-0 rounded-2…'%0A%0A- Expected%0A+ Received%0A%0A- className="absolute inset-0 rounded-2xl%0A+ "use client";%0A+%0A+ import { Check, Dumbbell, ExternalLink, Eye, Plus, Search, X } from "lucide-react";%0A+ import { useEffect, useMemo, useRef, useState } from "react";%0A+ import { useAuth } from "@/components/auth/auth-provider";%0A+ import { ExerciseMusclePreview } from "@/components/workouts/exercise-muscle-preview";%0A+ import { Badge } from "@/components/ui/badge";%0A+ import { Button } from "@/components/ui/button";%0A+ import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";%0A+ import { Input } from "@/components/ui/input";%0A+ import { Select } from "@/components/ui/select-field";%0A+ import { CardGridSkeleton } from "@/components/ui/state-views";%0A+ import { userSafeError } from "@/lib/error-formatting";%0A+ import { useTrainTranslation } from "@/lib/i18n/train";%0A+ import { formatExerciseDisplayList, formatExerciseDisplayValue, type ExerciseDisplayDomain } from "@/lib/train/exercise-display";%0A+ import { getMuscleIntelligenceCopy } from "@/lib/train/muscle-intelligence/muscle-intelligence-ui-copy";%0A+ import { createCatalogRequestGroupId } from "@/services/activity-catalog/client";%0A+ import {%0A+   emptyCanonicalWorkoutFilterOptions,%0A+   getCanonicalWorkoutFilterOptionsWithStatus,%0A+   getWorkoutsWithStatus,%0A+   mergeCanonicalWorkoutFilterOptions,%0A+   type CanonicalWorkoutFilterOptions,%0A+   type WorkoutFilterOption%0A+ } from "@/services/database/workout-library";%0A+ import {%0A+   getWorkoutReplacementEligibility,%0A+   isReplacementCandidateActionable,%0A+   replacementEligibilityMessage,%0A+   shouldClosePickerAfterAdd,%0A+   type ReplacementEligibility%0A+ } from "@/services/database/workout-replacement-eligibility";%0A+ import type { Workout } from "@/types";%0A+%0A+ function normalizeIdentityPart(value: string | null | undefined) {%0A+   return value?.trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "") ?? "";%0A+ }%0A+%0A+ function exerciseKey(exercise: Pick<Workout, "id" | "name" | "target_muscle" | "catalog_slug">) {%0A+   if (exercise.catalog_slug) return `catalog:${exercise.catalog_slug}`;%0A+   if (exercise.id) return `id:${exercise.id}`;%0A+   return `legacy:${normalizeIdentityPart(exercise.name)}:${normalizeIdentityPart(exercise.target_muscle)}`;%0A+ }%0A+%0A+ function optionLabel(options: WorkoutFilterOption[], value: string, language: "en" | "de" | "ar", domain: ExerciseDisplayDomain) {%0A+   return options.find((option) => option.value === value)?.label ?? formatExerciseDisplayValue(value, language, domain);%0A+ }%0A+%0A+ function isAbortError(error: unknown) {%0A+   return Boolean(error && typeof error === "object" && "name" in error && (error as { name?: unknown }).name === "AbortError");%0A+ }%0A+%0A+ const emptyPagination = { hasMore: false, nextOffset: null as number | null };%0A+%0A+ export function ExercisePickerDialog({ open, onOpenChange, dayName, existingKeys, onAdd, maxSelection }: {%0A+   open: boolean;%0A+   onOpenChange: (open: boolean) => void;%0A+   dayName: string;%0A+   existingKeys: string[];%0A+   onAdd: (exercises: Workout[]) => void;%0A+   maxSelection?: number;%0A+ }) {%0A+   const { user } = useAuth();%0A+   const { language, dir, locale, tr } = useTrainTranslation();%0A+   const muscleCopy = getMuscleIntelligenceCopy(language);%0A+   const libraryLoadFailedMessage = tr("libraryLoadFailed");%0A+   const replacementMode = maxSelection === 1;%0A+   const [query, setQuery] = useState("");%0A+   const [muscle, setMuscle] = useState("");%0A+   const [equipment, setEquipment] = useState("");%0A+   const [difficulty, setDifficulty] = useState("");%0A+   const [muscleCategory, setMuscleCategory] = useState("");%0A+   const [secondaryMuscle, setSecondaryMuscle] = useState("");%0A+   const [forceType, setForceType] = useState("");%0A+   const [exerciseType, setExerciseType] = useState("");%0A+   const [mechanics, setMechanics] = useState("");%0A+   const [results, setResults] = useState<Workout[]>([]);%0A+   const [filterOptions, setFilterOptions] = useState<CanonicalWorkoutFilterOptions>(() => emptyCanonicalWorkoutFilterOptions());%0A+   const [selected, setSelected] = useState<Map<string, Workout>>(new Map());%0A+   const [focusedExercise, setFocusedExercise] = useState<Workout | null>(null);%0A+   const [eligibility, setEligibility] = useState<Map<string, ReplacementEligibility>>(new Map());%0A+   const [eligibilityLoading, setEligibilityLoading] = useState(false);%0A+   const [eligibilityError, setEligibilityError] = useState("");%0A+   const [loading, setLoading] = useState(false);%0A+   const [loadingMore, setLoadingMore] = useState(false);%0A+   const [error, setError] = useState("");%0A+   const [loadMoreError, setLoadMoreError] = useState("");%0A+   const [pagination, setPagination] = useState(emptyPagination);%0A+   const returnFocusRef = useRef<HTMLElement | null>(null);%0A+   const initialRequestGroupConsumedRef = useRef<string | null>(null);%0A+   const activeGenerationGroupRef = useRef<string | null>(null);%0A+   const generationRef = useRef(0);%0A+   const eligibilityGenerationRef = useRef(0);%0A+   const filtersControllerRef = useRef<AbortController | null>(null);%0A+   const activitiesControllerRef = useRef<AbortController | null>(null);%0A+   const loadMoreControllerRef = useRef<AbortController | null>(null);%0A+   const initialCatalogRequestGroupId = useMemo(() => open ? createCatalogRequestGroupId() : null, [open]);%0A+   const existing = useMemo(() => new Set(existingKeys), [existingKeys]);%0A+%0A+   const activeFilters = useMemo(() => ({%0A+     primaryMuscles: muscle ? [muscle] : [],%0A+     equipmentRequired: equipment ? [equipment] : [],%0A+     difficulty: difficulty || undefined,%0A+     muscleCategories: muscleCategory ? [muscleCategory] : [],%0A+     secondaryMuscles: secondaryMuscle ? [secondaryMuscle] : [],%0A+     forceTypes: forceType ? [forceType] : [],%0A+     exerciseTypes: exerciseType ? [exerciseType] : [],%0A+     mechanics: mechanics ? [mechanics] : []%0A+   }), [difficulty, equipment, exerciseType, forceType, mechanics, muscle, muscleCategory, secondaryMuscle]);%0A+%0A+   useEffect(() => {%0A+     if (open || typeof document === "undefined") return;%0A+     const captureReturnTarget = () => {%0A+       const activeElement = document.activeElement;%0A+       if (activeElement instanceof HTMLElement && activeElement !== document.body && !activeElement.closest("[data-train-exercise-picker]")) {%0A+         returnFocusRef.current = activeElement;%0A+       }%0A+     };%0A+     captureReturnTarget();%0A+     document.addEventListener("focusin", captureReturnTarget, true);%0A+     return () => document.removeEventListener("focusin", captureReturnTarget, true);%0A+   }, [open]);%0A+%0A+   useEffect(() => {%0A+     if (!open || !initialCatalogRequestGroupId) return;%0A+     filtersControllerRef.current?.abort();%0A+     const controller = new AbortController();%0A+     filtersControllerRef.current = controller;%0A+     void getCanonicalWorkoutFilterOptionsWithStatus(locale, {%0A+       requestGroupId: initialCatalogRequestGroupId,%0A+       signal: controller.signal%0A+     }).then((result) => {%0A+       if (!controller.signal.aborted) setFilterOptions(result.data);%0A+     }).catch(() => {%0A+       // Result-derived options remain available if filter metadata fails.%0A+     });%0A+     return () => controller.abort();%0A+   }, [initialCatalogRequestGroupId, locale, open]);%0A+%0A+   useEffect(() => {%0A+     if (!open) return;%0A+     const generation = generationRef.current + 1;%0A+     generationRef.current = generation;%0A+     activitiesControllerRef.current?.abort();%0A+     loadMoreControllerRef.current?.abort();%0A+     const controller = new AbortController();%0A+     activitiesControllerRef.current = controller;%0A+     const initialGroupId = initialCatalogRequestGroupId;%0A+     const useInitialGroup = initialGroupId !== null && initialRequestGroupConsumedRef.current !== initialGroupId;%0A+     const requestGroupId = useInitialGroup ? initialGroupId : createCatalogRequestGroupId();%0A+     activeGenerationGroupRef.current = requestGroupId;%0A+     const timer = window.setTimeout(() => {%0A+       if (useInitialGroup && initialGroupId) initialRequestGroupConsumedRef.current = initialGroupId;%0A+       setLoading(true);%0A+       setLoadingMore(false);%0A+       setError("");%0A+       setLoadMoreError("");%0A+       setPagination(emptyPagination);%0A+       getWorkoutsWithStatus(query.trim(), activeFilters, 0, locale, {%0A+         requestGroupId,%0A+         signal: controller.signal%0A+       })%0A+         .then((result) => {%0A+           if (controller.signal.aborted || generationRef.current !== generation) return;%0A+           setResults(result.data);%0A+           setPagination(result.pagination ?? emptyPagination);%0A+           setFocusedExercise((current) => current && result.data.some((workout) => exerciseKey(workout) === exerciseKey(current)) ? current : null);%0A+           if (result.filterOptions) setFilterOptions((options) => mergeCanonicalWorkoutFilterOptions(options, result.filterOptions!));%0A+         })%0A+         .catch((loadError) => {%0A+           if (controller.signal.aborted || generationRef.current !== generation || isAbortError(loadError)) return;%0A+           setResults([]);%0A+           setFocusedExercise(null);%0A+           setPagination(emptyPagination);%0A+           setError(userSafeError(loadError, libraryLoadFailedMessage));%0A+         })%0A+         .finally(() => {%0A+           if (!controller.signal.aborted && generationRef.current === generation) setLoading(false);%0A+         });%0A+     }, 180);%0A+     return () => {%0A+       window.clearTimeout(timer);%0A+       controller.abort();%0A+     };%0A+   }, [activeFilters, initialCatalogRequestGroupId, libraryLoadFailedMessage, locale, open, query]);%0A+%0A+   useEffect(() => {%0A+     if (!open || !replacementMode) {%0A+       setEligibility(new Map());%0A+       setEligibilityLoading(false);%0A+       setEligibilityError("");%0A+       return;%0A+     }%0A+     const generation = eligibilityGenerationRef.current + 1;%0A+     eligibilityGenerationRef.current = generation;%0A+     if (!user?.id) {%0A+       setEligibility(new Map());%0A+       setEligibilityLoading(false);%0A+       setEligibilityError("Sign in again to verify replacement eligibility.");%0A+       return;%0A+     }%0A+     if (!results.length) {%0A+       setEligibility(new Map());%0A+       setEligibilityLoading(false);%0A+       setEligibilityError("");%0A+       return;%0A+     }%0A+     setEligibilityLoading(true);%0A+     setEligibilityError("");%0A+     void getWorkoutReplacementEligibility(%0A+       user.id,%0A+       results.map((workout) => ({ key: exerciseKey(workout), workout }))%0A+     ).then((next) => {%0A+       if (eligibilityGenerationRef.current === generation) setEligibility(next);%0A+     }).catch((loadError) => {%0A+       if (eligibilityGenerationRef.current !== generation) return;%0A+       setEligibility(new Map());%0A+       setEligibilityError(userSafeError(loadError, "Replacement eligibility could not be verified. Your current exercise was preserved."));%0A+     }).finally(() => {%0A+       if (eligibilityGenerationRef.current === generation) setEligibilityLoading(false);%0A+     });%0A+   }, [open, replacementMode, results, user?.id]);%0A+%0A+   useEffect(() => {%0A+     if (open) return;%0A+     filtersControllerRef.current?.abort();%0A+     activitiesControllerRef.current?.abort();%0A+     loadMoreControllerRef.current?.abort();%0A+     generationRef.current += 1;%0A+     eligibilityGenerationRef.current += 1;%0A+     initialRequestGroupConsumedRef.current = null;%0A+     activeGenerationGroupRef.current = null;%0A+     setSelected(new Map());%0A+     setFocusedExercise(null);%0A+     setResults([]);%0A+     setEligibility(new Map());%0A+     setEligibilityLoading(false);%0A+     setEligibilityError("");%0A+     setPagination(emptyPagination);%0A+     setLoading(false);%0A+     setLoadingMore(false);%0A+     setError("");%0A+     setLoadMoreError("");%0A+   }, [open]);%0A+%0A+   const muscleOptions = filterOptions.primaryMuscles;%0A+   const equipmentOptions = filterOptions.equipmentRequired;%0A+   const difficultyOptions = filterOptions.experienceLevels;%0A+   const muscleCategoryOptions = filterOptions.muscleCategories;%0A+   const secondaryMuscleOptions = filterOptions.secondaryMuscles;%0A+   const forceTypeOptions = filterOptions.forceTypes;%0A+   const exerciseTypeOptions = filterOptions.exerciseTypes;%0A+   const mechanicsOptions = filterOptions.mechanics;%0A+   const hasAdvancedOptions = Boolean(muscleCategoryOptions.length || secondaryMuscleOptions.length || forceTypeOptions.length || exerciseTypeOptions.length || mechanicsOptions.length);%0A+%0A+   function isActionable(key: string) {%0A+     if (!replacementMode) return true;%0A+     return isReplacementCandidateActionable(eligibility.get(key), eligibilityLoading, eligibilityError);%0A+   }%0A+%0A+   function toggle(exercise: Workout) {%0A+     const key = exerciseKey(exercise);%0A+     if (existing.has(key) || !isActionable(key)) return;%0A+     setSelected((current) => {%0A+       if (current.has(key)) {%0A+         const next = new Map(current);%0A+         next.delete(key);%0A+         return next;%0A+       }%0A+       const next = maxSelection === 1 ? new Map<string, Workout>() : new Map(current);%0A+       next.set(key, exercise);%0A+       return next;%0A+     });%0A+   }%0A+%0A+   function addSelected() {%0A+     const chosen = Array.from(selected.values());%0A+     if (!chosen.length) return;%0A+     onAdd(chosen);%0A+     if (shouldClosePickerAfterAdd(replacementMode)) onOpenChange(false);%0A+   }%0A+%0A+   function clearFilters() {%0A+     setQuery("");%0A+     setMuscle("");%0A+     setEquipment("");%0A+     setDifficulty("");%0A+     setMuscleCategory("");%0A+     setSecondaryMuscle("");%0A+     setForceType("");%0A+     setExerciseType("");%0A+     setMechanics("");%0A+   }%0A+%0A+   async function loadMore() {%0A+     const nextOffset = pagination.nextOffset;%0A+     if (!pagination.hasMore || nextOffset === null || loadingMore) return;%0A+     loadMoreControllerRef.current?.abort();%0A+     const controller = new AbortController();%0A+     loadMoreControllerRef.current = controller;%0A+     const generation = generationRef.current;%0A+     const requestGroupId = activeGenerationGroupRef.current ?? createCatalogRequestGroupId();%0A+     activeGenerationGroupRef.current = requestGroupId;%0A+     setLoadingMore(true);%0A+     setLoadMoreError("");%0A+     try {%0A+       const result = await getWorkoutsWithStatus(query.trim(), activeFilters, nextOffset, locale, {%0A+         requestGroupId,%0A+         signal: controller.signal%0A+       });%0A+       if (controller.signal.aborted || generationRef.current !== generation) return;%0A+       setResults((current) => {%0A+         const keys = new Set(current.map(exerciseKey));%0A+         return [...current, ...result.data.filter((exercise) => {%0A+           const key = exerciseKey(exercise);%0A+           if (keys.has(key)) return false;%0A+           keys.add(key);%0A+           return true;%0A+         })];%0A+       });%0A+       setPagination(result.pagination ?? emptyPagination);%0A+       if (result.filterOptions) setFilterOptions((options) => mergeCanonicalWorkoutFilterOptions(options, result.filterOptions!));%0A+     } catch (loadError) {%0A+       if (controller.signal.aborted || generationRef.current !== generation || isAbortError(loadError)) return;%0A+       setLoadMoreError(userSafeError(loadError, tr("moreExercisesLoadFallback")));%0A+     } finally {%0A+       if (!controller.signal.aborted && generationRef.current === generation) setLoadingMore(false);%0A+     }%0A+   }%0A+%0A+   const hasFilters = Boolean(query || muscle || equipment || difficulty || muscleCategory || secondaryMuscle || forceType || exerciseType || mechanics);%0A+   const activeFilterChips = [%0A+     muscle ? { id: "muscle", label: optionLabel(muscleOptions, muscle, language, "muscle"), clear: () => setMuscle("") } : null,%0A+     equipment ? { id: "equipment", label: optionLabel(equipmentOptions, equipment, language, "equipment"), clear: () => setEquipment("") } : null,%0A+     difficulty ? { id: "difficulty", label: optionLabel(difficultyOptions, difficulty, language, "difficulty"), clear: () => setDifficulty("") } : null,%0A+     muscleCategory ? { id: "muscle-category", label: optionLabel(muscleCategoryOptions, muscleCategory, language, "muscle"), clear: () => setMuscleCategory("") } : null,%0A+     secondaryMuscle ? { id: "secondary-muscle", label: optionLabel(secondaryMuscleOptions, secondaryMuscle, language, "muscle"), clear: () => setSecondaryMuscle("") } : null,%0A+     forceType ? { id: "force-type", label: optionLabel(forceTypeOptions, forceType, language, "force"), clear: () => setForceType("") } : null,%0A+     exerciseType ? { id: "exercise-type", label: optionLabel(exerciseTypeOptions, exerciseType, language, "category"), clear: () => setExerciseType("") } : null,%0A+     mechanics ? { id: "mechanics", label: optionLabel(mechanicsOptions, mechanics, language, "movement"), clear: () => setMechanics("") } : null%0A+   ].filter((chip): chip is { id: string; label: string; clear: () => void } => Boolean(chip));%0A+%0A+   return (%0A+     <Dialog open={open} onOpenChange={onOpenChange}>%0A+       <DialogContent%0A+         layout="responsive-drawer"%0A+         className="inset-x-0 bottom-0 top-0 h-dvh max-h-dvh w-screen max-w-none rounded-none border-0 sm:inset-x-0 sm:bottom-0 sm:top-0 sm:h-dvh sm:max-h-dvh sm:w-screen sm:max-w-none sm:translate-x-0 sm:translate-y-0 sm:rounded-none sm:p-0 lg:inset-y-0 lg:left-auto lg:right-0 lg:top-0 lg:h-dvh lg:max-h-dvh lg:w-[min(54rem,100vw)] lg:max-w-[54rem] lg:translate-x-0 lg:translate-y-0 lg:rounded-none lg:border-y-0 lg:border-l lg:rtl:left-0 lg:rtl:right-auto lg:rtl:border-l-0 lg:rtl:border-r"%0A+         closeLabel={tr("closePicker")}%0A+         dir={dir}%0A+         data-train-exercise-picker%0A+         onOpenAutoFocus={() => {%0A+           if (returnFocusRef.current?.isConnected) return;%0A+           const activeElement = document.activeElement;%0A+           if (activeElement instanceof HTMLElement && !activeElement.closest("[data-train-exercise-picker]")) returnFocusRef.current = activeElement;%0A+         }}%0A+         onCloseAutoFocus={(event) => {%0A+           const returnTarget = returnFocusRef.current;%0A+           if (!returnTarget?.isConnected) return;%0A+           event.preventDefault();%0A+           returnTarget.focus();%0A+           window.requestAnimationFrame(() => {%0A+             if (document.activeElement !== returnTarget) returnTarget.focus();%0A+           });%0A+         }}%0A+       >%0A+         <DialogHeader className="mb-0 shrink-0 border-b px-5 pb-4 pt-[calc(env(safe-area-inset-top)+1rem)] pe-16 text-start lg:pt-4">%0A+           <DialogTitle className="text-xl">{tr("addExercisesTo", { day: dayName })}</DialogTitle>%0A+           <DialogDescription className="font-medium text-foreground/70">{tr("selectionPreserved", { count: selected.size })}</DialogDescription>%0A+         </DialogHeader>%0A+%0A+         <div className="min-h-0 flex-1 overflow-y-auto overscroll-contain px-4 py-4 pb-32 sm:px-5" data-picker-scroll-region>%0A+           <div className="sticky -top-4 z-20 -mx-4 flex gap-2 overflow-x-auto border-b bg-background/95 px-4 py-3 backdrop-blur sm:-mx-5 sm:px-5 lg:grid lg:grid-cols-4 lg:gap-3 lg:overflow-visible" data-picker-filters>%0A+             <label className="relative min-w-[240px] shrink-0 lg:min-w-0"><span className="sr-only">{tr("searchExercises")}</span><Search className="pointer-events-none absolute start-3 top-3.5 h-4 w-4 text-muted-foreground" /><Input value={query} onChange={(event) => setQuery(event.target.value)} className="min-h-12 ps-10" placeholder={tr("searchExercises")} /></label>%0A+             <div className="min-w-[180px] shrink-0 lg:min-w-0"><Select className="h-12 w-full rounded-full border border-input bg-card px-4 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring" aria-label={tr("primaryMuscle")} value={muscle} onChange={setMuscle} placeholder={tr("primaryMuscle")} options={muscleOptions} /></div>%0A+             <div className="min-w-[180px] shrink-0 lg:min-w-0"><Select className="h-12 w-full rounded-full border border-input bg-card px-4 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring" aria-label={tr("equipment")} value={equipment} onChange={setEquipment} placeholder={tr("equipment")} options={equipmentOptions} /></div>%0A+             <div className="min-w-[180px] shrink-0 lg:min-w-0"><Select className="h-12 w-full rounded-full border border-input bg-card px-4 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring" aria-label={tr("difficulty")} value={difficulty} onChange={setDifficulty} placeholder={tr("difficulty")} options={difficultyOptions} /></div>%0A+             {hasFilters ? <Button type="button" variant="ghost" className="min-h-11 justify-self-start px-2 sm:col-span-2 lg:col-span-4" onClick={clearFilters}>{tr("clear")}</Button> : null}%0A+           </div>%0A+%0A+           {activeFilterChips.length ? <div className="mt-3 flex gap-2 overflow-x-auto" aria-label={tr("filters")}>{activeFilterChips.map((chip) => <button key={chip.id} type="button" className="inline-flex min-h-11 shrink-0 items-center gap-2 rounded-full border border-primary/30 bg-primary/5 px-3 text-sm font-medium" onClick={chip.clear}>{chip.label}<X className="h-4 w-4" aria-hidden="true" /></button>)}</div> : null}%0A+%0A+           {hasAdvancedOptions ? <details className="mt-3 rounded-2xl border border-border/70 bg-muted/20 px-3 py-2"><summary className="min-h-11 cursor-pointer select-none content-center font-medium focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring">{tr("moreFilters")}</summary><div className="mt-3 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{muscleCategoryOptions.length ? <Select aria-label={tr("muscleCategory")} value={muscleCategory} onChange={setMuscleCategory} placeholder={tr("muscleCategory")} options={muscleCategoryOptions} /> : null}{secondaryMuscleOptions.length ? <Select aria-label={tr("secondaryMuscle")} value={secondaryMuscle} onChange={setSecondaryMuscle} placeholder={tr("secondaryMuscle")} options={secondaryMuscleOptions} /> : null}{forceTypeOptions.length ? <Select aria-label={tr("forceType")} value={forceType} onChange={setForceType} placeholder={tr("forceType")} options={forceTypeOptions} /> : null}{exerciseTypeOptions.length ? <Select aria-label={tr("exerciseType")} value={exerciseType} onChange={setExerciseType} placeholder={tr("exerciseType")} options={exerciseTypeOptions} /> : null}{mechanicsOptions.length ? <Select aria-label={tr("mechanics")} value={mechanics} onChange={setMechanics} placeholder={tr("mechanics")} options={mechanicsOptions} /> : null}</div></details> : null}%0A+%0A+           <div className="mt-4"><ExerciseMusclePreview exercise={focusedExercise} language={language} /></div>%0A+           {loading ? <div className="mt-4"><CardGridSkeleton count={4} rows={3} /></div> : null}%0A+           {error ? <div role="alert" className="mt-4 rounded-2xl border border-destructive/30 bg-destructive/5 p-4 text-sm">{error}</div> : null}%0A+           {replacementMode && eligibilityError ? <div role="alert" className="mt-4 rounded-2xl border border-destructive/30 bg-destructive/5 p-4 text-sm">{eligibilityError}</div> : null}%0A+%0A+           {!loading && !error ? <><div className="mt-4 grid grid-cols-1 gap-3 lg:grid-cols-2" data-picker-results>{results.map((exercise) => {%0A+             const key = exerciseKey(exercise);%0A+             const duplicate = existing.has(key);%0A+             const isSelected = selected.has(key);%0A+             const isFocused = focusedExercise ? exerciseKey(focusedExercise) === key : false;%0A+             const candidateEligibility = eligibility.get(key);%0A+             const candidatePending = replacementMode && eligibilityLoading;%0A+             const ineligible = replacementMode && !candidatePending && candidateEligibility?.eligible !== true;%0A+             const disabled = duplicate || candidatePending || ineligible || Boolean(replacementMode && eligibilityError);%0A+             const guideUrl = exercise.custom_video_url || exercise.video_url || exercise.exercise_url;%0A+             return <article key={key} className={`flex min-h-[170px] flex-col rounded-2xl border p-4 ${disabled ? "border-border/60 bg-muted/30" : isSelected ? "border-primary bg-primary/5 ring-1 ring-primary/20" : isFocused ? "border-primary/50 bg-card" : "bg-card"}`} data-picker-exercise-card><div className="flex min-w-0 items-start justify-between gap-3"><div className="min-w-0"><h3 className="break-words text-base font-semibold leading-6">{exercise.name}</h3><p className="mt-1 break-words text-sm text-muted-foreground">{formatExerciseDisplayList(exercise.target_muscle, language, "muscle")} · {formatExerciseDisplayList(exercise.equipment, language, "equipment")}</p></div>{exercise.difficulty ? <Badge variant="outline" className="shrink-0">{formatExerciseDisplayValue(exercise.difficulty, language, "difficulty")}</Badge> : null}</div><p className="mt-3 line-clamp-2 text-sm leading-5 text-muted-foreground">{exercise.instructions}</p>{replacementMode && candidatePending ? <p className="mt-2 text-xs text-muted-foreground">Verifying tracked replacement eligibility…</p> : null}{replacementMode && ineligible ? <p className="mt-2 text-xs text-muted-foreground">{replacementEligibilityMessage(candidateEligibility?.reason)}</p> : null}<div className="mt-auto grid gap-2 pt-4 sm:grid-cols-2"><Button type="button" variant={isSelected ? "default" : "outline"} className="min-h-11 w-full" disabled={disabled} aria-pressed={isSelected} onClick={() => toggle(exercise)}>{duplicate || isSelected ? <Check className="h-4 w-4" /> : <Plus className="h-4 w-4" />}{duplicate ? tr("alreadyAdded") : isSelected ? tr("deselect") : ineligible ? "Unavailable" : candidatePending ? "Checking…" : tr("select")}</Button><Button type="button" variant={isFocused ? "default" : "outline"} className="min-h-11 w-full" aria-pressed={isFocused} onClick={() => setFocusedExercise(exercise)}><Eye className="h-4 w-4" />{muscleCopy.previewAction}</Button>{guideUrl ? <Button asChild type="button" variant="ghost" className="min-h-11 sm:col-span-2"><a href={guideUrl} target="_blank" rel="noreferrer"><ExternalLink className="h-4 w-4" />{tr("viewGuide")}</a></Button> : null}</div></article>;%0A+           })}</div>{results.length && (pagination.hasMore || loadMoreError) ? <div className="mt-5 flex flex-col items-center gap-3" data-picker-load-more>{loadMoreError ? <div role="alert" className="w-full rounded-2xl border border-destructive/30 bg-destructive/5 p-3 text-sm">{loadMoreError}</div> : null}{pagination.hasMore ? <Button type="button" variant="outline" className="min-h-12 min-w-40" disabled={loadingMore} onClick={() => void loadMore()}>{loadingMore ? tr("loadingLabel") : tr("loadMore")}</Button> : null}</div> : null}</> : null}%0A+           {!loading && !error && !results.length ? <div className="mt-4 grid min-h-40 place-items-center rounded-2xl border border-dashed text-center"><div><Dumbbell className="mx-auto mb-2 h-6 w-6 text-muted-foreground" /><p className="font-medium">{tr("noExercisesFound")}</p><p className="text-sm text-muted-foreground">{tr("broaderSearch")}</p></div></div> : null}%0A+         </div>%0A+%0A+         <div className="absolute inset-x-0 bottom-0 z-30 border-t bg-background/95 px-4 py-3 pb-[calc(env(safe-area-inset-bottom)+1rem)] backdrop-blur sm:px-5" data-picker-footer><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex min-h-11 items-center gap-2"><span className="text-sm font-semibold">{tr("selectedCount", { count: selected.size })}</span><Button type="button" variant="ghost" className="min-h-11" onClick={() => setSelected(new Map())} disabled={!selected.size}>{tr("clear")}</Button></div><Button type="button" className="min-h-[52px] w-full sm:w-auto sm:min-w-44" onClick={addSelected} disabled={!selected.size || (replacementMode && (eligibilityLoading || Boolean(eligibilityError)))}>{tr("addNExercises", { count: selected.size })}</Button></div></div>%0A+       </DialogContent>%0A+     </Dialog>%0A+   );%0A+ }%0A+%0A+ export { exerciseKey };%0A+%0A%0A ❯ lib/product/train-phase1-approved-ui.test.ts:42:20%0A%0A

::error file=/home/runner/work/gymsands/gymsands/lib/product/train-phase1-approved-ui.test.ts,title=lib/product/train-phase1-approved-ui.test.ts > approved Train Phase 1 UI contracts > localizes detail%2C history filters%2C direct-session failures%2C and the active workout controller,line=86,column=20::AssertionError: expected '"use client";\n\nimport { AlertTriang…' to contain 'const metadata = metadataLine(item.ta…'%0A%0A- Expected%0A+ Received%0A%0A- const metadata = metadataLine(item.target_muscle, item.equipment)%0A+ "use client";%0A+%0A+ import { AlertTriangle, CheckCircle2, ExternalLink, Heart, Play, RotateCcw, Save, TrendingUp, ChevronDown } from "lucide-react";%0A+ import { useParams } from "next/navigation";%0A+ import { useCallback, useEffect, useMemo, useState } from "react";%0A+ import { Button } from "@/components/ui/button";%0A+ import { Badge } from "@/components/ui/badge";%0A+ import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";%0A+ import { Input } from "@/components/ui/input";%0A+ import { Label } from "@/components/ui/label";%0A+ import { CardSkeleton, ErrorState } from "@/components/ui/state-views";%0A+ import { PageHeading } from "@/components/layout/page-heading";%0A+ import { TrainPageContainer } from "@/components/workouts/train-ui";%0A+ import { ExerciseVideoPlayer } from "@/components/workouts/video-player";%0A+ import { useAuth } from "@/components/auth/auth-provider";%0A+ import { useToast } from "@/components/ui/toaster";%0A+ import Link from "next/link";%0A+ import { getExerciseVideos, getUserExerciseVideo, getWorkout, getWorkoutAlternatives, resetUserExerciseVideo, upsertUserExerciseVideo } from "@/services/database/workout-library";%0A+ import { getWorkoutHistoryDetailed } from "@/services/database/workout-sessions";%0A+ import { getCustomExercisesWithStatus, getFavoriteExerciseIdsWithStatus, setFavoriteExercise } from "@/services/workouts/exercise-library-store";%0A+ import { findExerciseVideo } from "@/services/workouts/video-matching";%0A+ import { userSafeError } from "@/lib/error-formatting";%0A+ import { cn } from "@/lib/utils";%0A+ import { useTrainTranslation } from "@/lib/i18n/train";%0A+ import { formatExerciseDisplayList, formatExerciseDisplayValue } from "@/lib/train/exercise-display";%0A+ import type { ExerciseLog, ExerciseVideo, Workout, WorkoutSessionSummary } from "@/types";%0A+%0A+ function normalizeExerciseName(value: string) {%0A+   return value.toLowerCase().replace(/^[a-z]\d\s*[:.)-]\s*/i, "").replace(/[^a-z0-9]+/g, " ").trim();%0A+ }%0A+%0A+ function estimateOneRepMax(weightKg: number, reps: number) {%0A+   if (weightKg <= 0 || reps <= 0) return 0;%0A+   return Math.round(weightKg * (1 + reps / 30) * 10) / 10;%0A+ }%0A+%0A+ function round(value: number) {%0A+   return Math.round(value * 10) / 10;%0A+ }%0A+%0A+ function exerciseLogsFor(history: WorkoutSessionSummary[], workout: Pick<Workout, "id" | "name">) {%0A+   const target = normalizeExerciseName(workout.name);%0A+   return history.flatMap((session) =>%0A+     (session.exercise_logs ?? [])%0A+       .filter((log) => {%0A+         if (log.source_workout_id) return log.source_workout_id === workout.id;%0A+         if (session.workout_id) return session.workout_id === workout.id;%0A+         return normalizeExerciseName(log.exercise_name) === target;%0A+       })%0A+       .map((log) => ({ ...log, sessionDate: session.completed_at || session.started_at }))%0A+   );%0A+ }%0A+%0A+ function buildExerciseHistory(logs: Array<ExerciseLog & { sessionDate: string }>) {%0A+   const valid = logs.filter((log) => Number(log.reps ?? 0) > 0 || Number(log.weight_kg ?? 0) > 0);%0A+   const bestWeight = Math.max(0, ...valid.map((log) => Number(log.weight_kg ?? 0)));%0A+   const bestReps = Math.max(0, ...valid.map((log) => Number(log.reps ?? 0)));%0A+   const bestOneRepMax = Math.max(0, ...valid.map((log) => estimateOneRepMax(Number(log.weight_kg ?? 0), Number(log.reps ?? 0))));%0A+   const bestSet = [...valid].sort((a, b) => (Number(b.weight_kg ?? 0) * Number(b.reps ?? 0)) - (Number(a.weight_kg ?? 0) * Number(a.reps ?? 0)))[0] ?? null;%0A+   const volumeByDate = new Map<string, number>();%0A+   const bestSetByDate = new Map<string, number>();%0A+   const oneRmByDate = new Map<string, number>();%0A+%0A+   valid.forEach((log) => {%0A+     const date = log.sessionDate.slice(0, 10);%0A+     const volume = Number(log.weight_kg ?? 0) * Number(log.reps ?? 0);%0A+     const oneRm = estimateOneRepMax(Number(log.weight_kg ?? 0), Number(log.reps ?? 0));%0A+     volumeByDate.set(date, (volumeByDate.get(date) ?? 0) + volume);%0A+     bestSetByDate.set(date, Math.max(bestSetByDate.get(date) ?? 0, volume));%0A+     oneRmByDate.set(date, Math.max(oneRmByDate.get(date) ?? 0, oneRm));%0A+   });%0A+%0A+   const trends = Array.from(volumeByDate.entries()).sort(([a], [b]) => a.localeCompare(b)).map(([date, volume]) => ({%0A+     date,%0A+     volume: round(volume),%0A+     bestSetVolume: round(bestSetByDate.get(date) ?? 0),%0A+     estimatedOneRepMax: round(oneRmByDate.get(date) ?? 0)%0A+   }));%0A+%0A+   return { count: valid.length, bestWeight, bestReps, bestOneRepMax: round(bestOneRepMax), bestSet, trends };%0A+ }%0A+%0A+ function sameText(a: string | null | undefined, b: string | null | undefined) {%0A+   const left = normalizeExerciseName(a ?? "");%0A+   const right = normalizeExerciseName(b ?? "");%0A+   return Boolean(left && right && left === right);%0A+ }%0A+%0A+ function hasInvalidUrl(value: string | null | undefined) {%0A+   const clean = value?.trim();%0A+   return Boolean(clean && !/^https?:\/\/[^\s]+$/i.test(clean));%0A+ }%0A+%0A+ function metadataLine(...values: Array<string | null | undefined>) {%0A+   return values.map((value) => value?.trim()).filter(Boolean).join(" · ");%0A+ }%0A+%0A+ export default function WorkoutDetailsPage() {%0A+   const params = useParams<{ id: string }>();%0A+   const { user } = useAuth();%0A+   const userId = user?.id;%0A+   const { toast } = useToast();%0A+   const { language, dir, locale, tr } = useTrainTranslation();%0A+   const [workout, setWorkout] = useState<Workout | null>(null);%0A+   const [video, setVideo] = useState<ExerciseVideo | null>(null);%0A+   const [customVideoUrl, setCustomVideoUrl] = useState("");%0A+   const [customVideoDraft, setCustomVideoDraft] = useState("");%0A+   const [isSavingVideo, setIsSavingVideo] = useState(false);%0A+   const [loadError, setLoadError] = useState("");%0A+   const [loadWarning, setLoadWarning] = useState("");%0A+   const [isLoading, setIsLoading] = useState(true);%0A+   const [history, setHistory] = useState<WorkoutSessionSummary[]>([]);%0A+   const [alternatives, setAlternatives] = useState<Workout[]>([]);%0A+   const [favoriteIds, setFavoriteIds] = useState<string[]>([]);%0A+   const [isFavoritePending, setIsFavoritePending] = useState(false);%0A+   const [favoriteError, setFavoriteError] = useState("");%0A+   const [videoStatus, setVideoStatus] = useState<"idle" | "saving" | "saved" | "failed" | "reset">("idle");%0A+   const [videoMessage, setVideoMessage] = useState("");%0A+%0A+   const load = useCallback(async () => {%0A+     setIsLoading(true);%0A+     setLoadError("");%0A+     setLoadWarning("");%0A+     try {%0A+       const warnings: string[] = [];%0A+       const customResult = await getCustomExercisesWithStatus(userId);%0A+       if (customResult.status.source === "degraded" && customResult.status.message) warnings.push(customResult.status.message);%0A+       const customExercise = customResult.data.find((exercise) => exercise.id === params.id) ?? null;%0A+       const nextWorkout = customExercise ?? await getWorkout(params.id, locale);%0A+       const [videosResult, customVideoResult, historyResult, alternativesResult, favoritesResult] = await Promise.allSettled([%0A+         getExerciseVideos(nextWorkout.name, locale),%0A+         userId && !customExercise ? getUserExerciseVideo(userId, nextWorkout.id) : Promise.resolve(null),%0A+         userId ? getWorkoutHistoryDetailed(userId, 150) : Promise.resolve([]),%0A+         getWorkoutAlternatives(nextWorkout.id, 6, locale),%0A+         getFavoriteExerciseIdsWithStatus(userId)%0A+       ]);%0A+       const videos = videosResult.status === "fulfilled" ? videosResult.value : [];%0A+       if (videosResult.status === "rejected") warnings.push(tr("exerciseVideoLoadWarning"));%0A+       const customVideo = customVideoResult.status === "fulfilled" ? customVideoResult.value : null;%0A+       if (customVideoResult.status === "rejected") warnings.push(tr("customVideoLoadWarning"));%0A+       const workoutHistory = historyResult.status === "fulfilled" ? historyResult.value : [];%0A+       if (historyResult.status === "rejected") warnings.push(tr("exerciseHistoryLoadWarning"));%0A+       const libraryAlternatives = alternativesResult.status === "fulfilled" ? alternativesResult.value.data : [];%0A+       if (alternativesResult.status === "fulfilled" && alternativesResult.value.status.message) warnings.push(alternativesResult.value.status.message);%0A+       if (alternativesResult.status === "rejected") warnings.push(tr("alternativesLoadWarning"));%0A+       if (favoritesResult.status === "fulfilled") {%0A+         setFavoriteIds(favoritesResult.value.data);%0A+         if (favoritesResult.value.status.source === "degraded" && favoritesResult.value.status.message) warnings.push(favoritesResult.value.status.message);%0A+       } else {%0A+         warnings.push(tr("favoriteLoadWarning"));%0A+         setFavoriteIds([]);%0A+       }%0A+       const localCustomAlternatives = customResult.data%0A+         .filter((item) => item.id !== nextWorkout.id)%0A+         .filter((item) => sameText(item.target_muscle, nextWorkout.target_muscle) || sameText(item.equipment, nextWorkout.equipment) || sameText(item.mechanics, nextWorkout.mechanics));%0A+       const combinedAlternatives = [...localCustomAlternatives, ...libraryAlternatives]%0A+         .filter((item) => item.id !== nextWorkout.id)%0A+         .filter((item, index, all) => all.findIndex((match) => match.id === item.id) === index)%0A+         .slice(0, 6);%0A+       setWorkout(nextWorkout);%0A+       setVideo(findExerciseVideo(nextWorkout, videos));%0A+       setCustomVideoUrl(customExercise?.custom_video_url ?? customVideo?.custom_video_url ?? "");%0A+       setCustomVideoDraft(customExercise?.custom_video_url ?? customVideo?.custom_video_url ?? "");%0A+       setHistory(workoutHistory);%0A+       setAlternatives(combinedAlternatives);%0A+       setLoadWarning(Array.from(new Set(warnings)).join(" "));%0A+     } catch (error) {%0A+       const message = userSafeError(error, tr("workoutDetailsLoadFailed"));%0A+       setLoadError(message);%0A+       toast({ title: tr("couldNotLoadWorkout"), description: message });%0A+     } finally {%0A+       setIsLoading(false);%0A+     }%0A+   }, [locale, params.id, toast, tr, userId]);%0A+%0A+   useEffect(() => {%0A+     void load();%0A+   }, [load]);%0A+%0A+   const exerciseHistory = useMemo(() => workout ? buildExerciseHistory(exerciseLogsFor(history, workout)) : null, [history, workout]);%0A+%0A+   if (isLoading) return <CardSkeleton rows={7} />;%0A+%0A+   if (loadError) {%0A+     return (%0A+       <ErrorState%0A+         title={tr("planUnavailable")}%0A+         description={loadError}%0A+         onRetry={load}%0A+         fallbackLabel={tr("exerciseLibrary")}%0A+         fallbackHref="/workouts"%0A+       />%0A+     );%0A+   }%0A+%0A+   if (!workout) {%0A+     return (%0A+       <ErrorState%0A+         title={tr("workoutNotFound")}%0A+         description={tr("exerciseNotFoundDescription")}%0A+         fallbackLabel={tr("exerciseLibrary")}%0A+         fallbackHref="/workouts"%0A+       />%0A+     );%0A+   }%0A+%0A+   const guideUrl = workout.exercise_url || (workout.notes?.startsWith("http") ? workout.notes : video?.exercise_url);%0A+   const videoUrl = customVideoUrl || null;%0A+   const displayVideo = video%0A+     ? { ...video, exercise_url: guideUrl ?? video.exercise_url, video_url: customVideoUrl || video.video_url }%0A+     : videoUrl%0A+       ? { id: workout.id, exercise_name: workout.name, category_type: workout.category, category: workout.target_muscle, exercise_url: guideUrl ?? "", video_url: customVideoUrl, instructions: workout.instructions, source: "user_custom", is_global: true }%0A+       : null;%0A+   const secondaryMuscles = workout.secondary_muscles ?? video?.secondary_muscles ?? [];%0A+   const displayTarget = formatExerciseDisplayList(workout.target_muscle || workout.muscle_category, language, "muscle");%0A+   const displayEquipment = formatExerciseDisplayList(workout.equipment_required || workout.equipment, language, "equipment");%0A+   const displayDifficulty = formatExerciseDisplayValue(workout.experience_level || workout.difficulty, language, "difficulty");%0A+   const displayMovement = formatExerciseDisplayValue(workout.movement_pattern || workout.mechanics || workout.category, language, "movement");%0A+   const displayForce = formatExerciseDisplayValue(workout.force_type, language, "force");%0A+   const displaySecondary = formatExerciseDisplayList(secondaryMuscles, language, "muscle");%0A+   const favorite = favoriteIds.includes(workout.id);%0A+   const mistakes = workout.notes && !workout.notes.startsWith("http") && /mistake|avoid|form/i.test(workout.notes) ? workout.notes : tr("noneSaved");%0A+   const customVideoInvalid = hasInvalidUrl(customVideoDraft);%0A+%0A+   async function saveCustomVideo() {%0A+     if (!workout) return;%0A+     if (!userId) {%0A+       const message = tr("customVideoSignIn");%0A+       setVideoStatus("failed");%0A+       setVideoMessage(message);%0A+       toast({ title: tr("signInRequired"), description: message });%0A+       return;%0A+     }%0A+     if (customVideoInvalid) {%0A+       setVideoStatus("failed");%0A+       setVideoMessage(tr("invalidCustomVideoUrl"));%0A+       return;%0A+     }%0A+     try {%0A+       setIsSavingVideo(true);%0A+       setVideoStatus("saving");%0A+       setVideoMessage("");%0A+       const saved = await upsertUserExerciseVideo(userId, workout.id, customVideoDraft);%0A+       setCustomVideoUrl(saved.custom_video_url);%0A+       setCustomVideoDraft(saved.custom_video_url);%0A+       setVideoStatus("saved");%0A+       setVideoMessage(tr("customVideoSavedMessage"));%0A+       toast({ title: tr("customVideoSavedTitle"), description: tr("customVideoSavedDescription") });%0A+     } catch (error) {%0A+       const message = userSafeError(error, tr("checkVideoLink"));%0A+       setVideoStatus("failed");%0A+       setVideoMessage(message);%0A+       toast({ title: tr("customVideoSaveFailed"), description: message });%0A+     } finally {%0A+       setIsSavingVideo(false);%0A+     }%0A+   }%0A+%0A+   async function resetCustomVideo() {%0A+     if (!workout || !userId) return;%0A+     try {%0A+       setIsSavingVideo(true);%0A+       setVideoStatus("saving");%0A+       setVideoMessage("");%0A+       await resetUserExerciseVideo(userId, workout.id);%0A+       setCustomVideoUrl("");%0A+       setCustomVideoDraft("");%0A+       setVideoStatus("reset");%0A+       setVideoMessage(tr("customVideoClearedMessage"));%0A+       toast({ title: tr("customVideoClearedTitle"), description: tr("customVideoClearedDescription") });%0A+     } catch (error) {%0A+       const message = userSafeError(error, tr("customVideoResetFailedMessage"));%0A+       setVideoStatus("failed");%0A+       setVideoMessage(message);%0A+       toast({ title: tr("customVideoResetFailedTitle"), description: message });%0A+     } finally {%0A+       setIsSavingVideo(false);%0A+     }%0A+   }%0A+%0A+   async function toggleFavorite() {%0A+     const currentWorkout = workout;%0A+     if (!currentWorkout || isFavoritePending) return;%0A+     const previousIds = favoriteIds;%0A+     const nextFavorite = !favorite;%0A+     const optimistic = nextFavorite%0A+       ? Array.from(new Set([...favoriteIds, currentWorkout.id]))%0A+       : favoriteIds.filter((id) => id !== currentWorkout.id);%0A+     setIsFavoritePending(true);%0A+     setFavoriteError("");%0A+     setFavoriteIds(optimistic);%0A+     try {%0A+       const next = await setFavoriteExercise(userId, currentWorkout.id, nextFavorite);%0A+       setFavoriteIds(next);%0A+       toast({ title: nextFavorite ? tr("exerciseFavorited") : tr("exerciseUnfavorited"), description: currentWorkout.name });%0A+     } catch (error) {%0A+       const message = userSafeError(error, tr("favoriteRestoreMessage"));%0A+       setFavoriteIds(previousIds);%0A+       setFavoriteError(message);%0A+       toast({ title: tr("favoriteNotSaved"), description: message });%0A+     } finally {%0A+       setIsFavoritePending(false);%0A+     }%0A+   }%0A+%0A+   return (%0A+     <TrainPageContainer className="space-y-6" dir={dir}>%0A+       <PageHeading%0A+         title={workout.name}%0A+         description={metadataLine(displayTarget, displayEquipment, displayDifficulty)}%0A+         action={%0A+           <div className="flex flex-wrap gap-2">%0A+             <Button className="min-h-12" variant={favorite ? "default" : "outline"} onClick={toggleFavorite} disabled={isFavoritePending} aria-busy={isFavoritePending}>%0A+               <Heart className={cn("h-4 w-4", favorite && "fill-current")} />%0A+               {isFavoritePending ? tr("saving") : favorite ? tr("savedLabel") : tr("favorite")}%0A+             </Button>%0A+             <Button asChild className="min-h-12">%0A+               <Link href={`/workouts/session/${workout.id}`}><Play className="h-4 w-4" /> {tr("startSession")}</Link>%0A+             </Button>%0A+           </div>%0A+         }%0A+       />%0A+       {loadWarning ? <InlineStatus tone="warning" title={tr("degradedExerciseData")} description={loadWarning} /> : null}%0A+       {favoriteError ? <InlineStatus tone="error" title={tr("favoriteNotSaved")} description={favoriteError} /> : null}%0A+       <div className="grid gap-4 lg:grid-cols-[0.9fr_1.1fr]">%0A+         <div className="space-y-4">%0A+           <Card>%0A+             <CardHeader><CardTitle>{tr("details")}</CardTitle></CardHeader>%0A+             <CardContent className="space-y-5">%0A+               {!workout.is_global ? <div><Badge variant="success">{tr("custom")}</Badge></div> : null}%0A+%0A+               <div className="grid gap-3 sm:grid-cols-2">%0A+                 <Detail label={tr("primaryMuscle")} value={displayTarget || tr("noneSaved")} />%0A+                 <Detail label={tr("secondaryMuscle")} value={displaySecondary || tr("noneSaved")} />%0A+                 <Detail label={tr("equipment")} value={displayEquipment || tr("noneSaved")} />%0A+                 <Detail label={tr("mechanics")} value={displayMovement || tr("noneSaved")} />%0A+                 <Detail label={tr("forceType")} value={displayForce || tr("noneSaved")} />%0A+                 <Detail label={tr("difficulty")} value={displayDifficulty || tr("noneSaved")} />%0A+               </div>%0A+%0A+               <TextPanel title={tr("instructions")} text={video?.instructions || workout.instructions} fallback={tr("noneSaved")} />%0A+               <TextPanel title={tr("mistakesToAvoid")} text={mistakes} fallback={tr("noneSaved")} />%0A+%0A+               <div className="grid gap-2 sm:grid-cols-2">%0A+                 {guideUrl ? <Button asChild variant="outline" className="min-h-12"><a href={guideUrl} target="_blank" rel="noreferrer"><ExternalLink className="h-4 w-4" /> {tr("openExerciseGuide")}</a></Button> : <Button type="button" variant="outline" className="min-h-12" disabled>{tr("noGuide")}</Button>}%0A+                 {customVideoUrl ? <Button asChild variant="outline" className="min-h-12"><a href={customVideoUrl} target="_blank" rel="noreferrer"><ExternalLink className="h-4 w-4" /> {tr("openCustomVideo")}</a></Button> : <Button type="button" variant="outline" className="min-h-12" disabled>{tr("noCustomVideo")}</Button>}%0A+               </div>%0A+%0A+               <div className="rounded-md border p-3">%0A+                 <Label htmlFor="custom-video-url">{tr("customVideoUrl")}</Label>%0A+                 <div className="mt-2 grid gap-2 sm:grid-cols-[1fr_auto_auto]">%0A+                   <Input%0A+                     id="custom-video-url"%0A+                     value={customVideoDraft}%0A+                     onChange={(event) => {%0A+                       setCustomVideoDraft(event.target.value);%0A+                       setVideoStatus("idle");%0A+                       setVideoMessage("");%0A+                     }}%0A+                     placeholder="https://youtube.com/watch?v=..."%0A+                     className="h-12"%0A+                     aria-invalid={customVideoInvalid}%0A+                   />%0A+                   <Button type="button" className="min-h-12" onClick={saveCustomVideo} disabled={isSavingVideo || !customVideoDraft.trim() || customVideoInvalid}><Save className="h-4 w-4" /> {isSavingVideo && videoStatus === "saving" ? tr("saving") : tr("save")}</Button>%0A+                   <Button type="button" variant="outline" className="min-h-12" onClick={resetCustomVideo} disabled={isSavingVideo || !customVideoUrl}><RotateCcw className="h-4 w-4" /> {tr("reset")}</Button>%0A+                 </div>%0A+                 {customVideoInvalid ? <p className="mt-2 text-sm leading-6 text-destructive">{tr("invalidCustomVideoUrl")}</p> : null}%0A+                 {videoMessage ? <InlineStatus className="mt-3" tone={videoStatus === "failed" ? "error" : "success"} title={videoStatus === "failed" ? tr("customVideoSaveFailed") : videoStatus === "reset" ? tr("customVideoClearedTitle") : tr("customVideoSavedTitle")} description={videoMessage} /> : null}%0A+               </div>%0A+             </CardContent>%0A+           </Card>%0A+%0A+           <Card>%0A+             <CardHeader><CardTitle className="flex items-center gap-2"><TrendingUp className="h-5 w-5 text-primary" /> {tr("realWorkoutHistory")}</CardTitle></CardHeader>%0A+             <CardContent className="space-y-4">%0A+               {!exerciseHistory?.count ? <p className="text-sm text-muted-foreground">{tr("noWorkoutHistory")}</p> : null}%0A+               {exerciseHistory?.count ? (%0A+                 <>%0A+                   <div className="grid gap-3 sm:grid-cols-3">%0A+                     <Detail label={tr("bestWeight")} value={`${exerciseHistory.bestWeight} kg`} />%0A+                     <Detail label={tr("bestReps")} value={`${exerciseHistory.bestReps}`} />%0A+                     <Detail label={tr("estimatedOneRepMax")} value={`${exerciseHistory.bestOneRepMax} kg`} />%0A+                   </div>%0A+                   <Detail label={tr("bestSet")} value={exerciseHistory.bestSet ? `${exerciseHistory.bestSet.weight_kg ?? 0} kg × ${exerciseHistory.bestSet.reps ?? 0}` : tr("noSetData")} />%0A+                   {exerciseHistory.trends.length < 2 ? <p className="text-sm text-muted-foreground">{tr("notEnoughHistory")}</p> : <div className="space-y-2">{exerciseHistory.trends.slice(-6).map((point) => <div key={point.date} className="rounded-md border p-3 text-sm"><p className="font-semibold">{point.date}</p><p className="text-muted-foreground">{point.volume} kg · {point.bestSetVolume} kg · 1RM {point.estimatedOneRepMax} kg</p></div>)}</div>}%0A+                 </>%0A+               ) : null}%0A+             </CardContent>%0A+           </Card>%0A+%0A+           <Card>%0A+             <CardHeader><CardTitle>{tr("alternativeExercises")}</CardTitle></CardHeader>%0A+             <CardContent className="grid gap-3 sm:grid-cols-2">%0A+               {!alternatives.length ? <p className="text-sm text-muted-foreground">{tr("noAlternatives")}</p> : null}%0A+               {alternatives.map((item) => {%0A+                 const metadata = metadataLine(formatExerciseDisplayList(item.target_muscle, language, "muscle"), formatExerciseDisplayList(item.equipment, language, "equipment"));%0A+                 return <Link key={item.id} href={`/workouts/${item.id}`} className="min-h-12 rounded-md border p-3 transition hover:border-primary"><p className="font-semibold">{item.name}</p>{metadata ? <p className="text-sm text-muted-foreground">{metadata}</p> : null}</Link>;%0A+               })}%0A+             </CardContent>%0A+           </Card>%0A+         </div>%0A+         <ExerciseVideoPlayer video={displayVideo} />%0A+       </div>%0A+     </TrainPageContainer>%0A+   );%0A+ }%0A+%0A+ function TextPanel({ title, text, fallback }: { title: string; text: string | null | undefined; fallback: string }) {%0A+   return (%0A+     <details className="group rounded-md bg-muted/40">%0A+       <summary className="flex min-h-12 cursor-pointer list-none items-center justify-between gap-2 p-3 text-sm font-semibold text-foreground">%0A+         {title}%0A+         <ChevronDown className="h-4 w-4 text-muted-foreground transition group-open:rotate-180" />%0A+       </summary>%0A+       <p className="px-3 pb-3 leading-7 text-muted-foreground">{text || fallback}</p>%0A+     </details>%0A+   );%0A+ }%0A+%0A+ function Detail({ label, value }: { label: string | null | undefined; value: string | null | undefined }) {%0A+   return <div className="rounded-md bg-muted/40 p-3"><p className="text-xs font-semibold uppercase tracking-normal text-muted-foreground">{label}</p><p className="mt-1 text-sm font-medium text-foreground">{value || "N/A"}</p></div>;%0A+ }%0A+%0A+ function InlineStatus({%0A+   tone,%0A+   title,%0A+   description,%0A+   className%0A+ }: {%0A+   tone: "success" | "warning" | "error";%0A+   title: string;%0A+   description: string;%0A+   className?: string;%0A+ }) {%0A+   const Icon = tone === "success" ? CheckCircle2 : AlertTriangle;%0A+   const toneClass = tone === "error"%0A+     ? "border-destructive/30 bg-destructive/5"%0A+     : tone === "warning"%0A+       ? "border-warning/30 bg-warning/10"%0A+       : "border-success/30 bg-success/5";%0A+   const iconClass = tone === "error"%0A+     ? "text-destructive"%0A+     : tone === "warning"%0A+       ? "text-warning"%0A+       : "text-success";%0A+%0A+   return (%0A+     <Card className={cn(toneClass, className)}>%0A+       <CardContent className="flex items-start gap-3 p-4">%0A+         <Icon className={cn("mt-0.5 h-5 w-5 shrink-0", iconClass)} />%0A+         <div>%0A+           <p className="font-semibold text-foreground">{title}</p>%0A+           <p className="mt-1 text-sm leading-6 text-muted-foreground">{description}</p>%0A+         </div>%0A+       </CardContent>%0A+     </Card>%0A+   );%0A+ }%0A+%0A%0A ❯ lib/product/train-phase1-approved-ui.test.ts:86:20%0A%0A

::error file=/home/runner/work/gymsands/gymsands/lib/product/train-phase2a-architecture.test.ts,title=lib/product/train-phase2a-architecture.test.ts > Train Phase 2A architecture contract > enforces privacy%2C ownership%2C JSON shape%2C and verification in the authoritative quality gate,line=96,column=22::AssertionError: expected 'name: Quality\n\non:\n  pull_request:…' to contain '-f supabase/verification/train-phase2…'%0A%0A- Expected%0A+ Received%0A%0A- -f supabase/verification/train-phase2a-program-architecture.sql%0A+ name: Quality%0A+%0A+ on:%0A+   pull_request:%0A+     branches:%0A+       - main%0A+   workflow_dispatch:%0A+%0A+ permissions:%0A+   contents: read%0A+%0A+ concurrency:%0A+   group: quality-${{ github.workflow }}-${{ github.ref }}%0A+   cancel-in-progress: true%0A+%0A+ jobs:%0A+   verify:%0A+     runs-on: ubuntu-latest%0A+     timeout-minutes: 30%0A+     env:%0A+       NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co%0A+       NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-public-key-long-enough%0A+       SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-service-role-key-long-enough%0A+       NEXT_PUBLIC_APP_URL: http://localhost:3000%0A+       NEXT_PUBLIC_USE_MOCK_AUTH: "true"%0A+       PLAIVRA_MCP_BASE_URL: http://localhost:3000/api/mcp%0A+       PLAIVRA_MCP_TOKEN_SECRET: ci-placeholder-token-secret-that-is-long-enough%0A+       PLAIVRA_CHATGPT_REDIRECT_URIS: https://chatgpt.com/connector_platform_oauth_redirect%0A+       PLAIVRA_CIMD_ALLOWED_ORIGINS: https://chatgpt.com%0A+       PLAIVRA_COMMIT_SHA: ${{ github.event.pull_request.head.sha || github.sha }}%0A+       PLAIVRA_RELEASE_ENVIRONMENT: ci%0A+       PLAIVRA_SCHEMA_COMPATIBILITY_VERSION: "2"%0A+%0A+     steps:%0A+       - name: Checkout exact revision%0A+         uses: actions/checkout@v4%0A+         with:%0A+           ref: ${{ github.event.pull_request.head.sha || github.sha }}%0A+           fetch-depth: 0%0A+%0A+       - name: Verify checkout identity%0A+         shell: bash%0A+         run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha || github.sha }}"%0A+%0A+       - name: Detect relevant verification scope%0A+         id: scope%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             changed_files="$(git ls-files)"%0A+           else%0A+             base_sha="${{ github.event.pull_request.base.sha }}"%0A+             changed_files="$(git diff --name-only "${base_sha}...HEAD")"%0A+           fi%0A+%0A+           printf '%25s\n' "$changed_files"%0A+%0A+           code=false%0A+           runtime=false%0A+           dependencies=false%0A+           database=false%0A+           integration=false%0A+           scripts=false%0A+           environment=false%0A+           ui=false%0A+           train_ui=false%0A+%0A+           if grep -Eq '\.(ts|tsx|js|jsx|mjs|cjs)$|^(package(-lock)?\.json|tsconfig\.json|vitest\..*\.mjs|eslint\.config\.)' <<< "$changed_files"; then%0A+             code=true%0A+           fi%0A+%0A+           if grep -Eq '^(app/|components/|lib/|services/|types/|middleware\.|next\.config\.|package\.json)' <<< "$changed_files" \%0A+             && grep -Eqv '(\.test\.|\.spec\.|^docs/)' <<< "$changed_files"; then%0A+             runtime=true%0A+           fi%0A+%0A+           if grep -Eq '^package-lock\.json$' <<< "$changed_files"; then dependencies=true; fi%0A+           if grep -Eq '^(supabase/migrations/|supabase/migration-ledger\.json|scripts/(check-migration-ledger|test-database-preflight-control)|services/database/|types/database\.ts)' <<< "$changed_files"; then database=true; fi%0A+           if grep -Eq '^(services/database/|app/api/|supabase/migrations/|vitest\.integration\.config\.mjs)' <<< "$changed_files"; then integration=true; fi%0A+           if grep -Eq '^scripts/' <<< "$changed_files"; then scripts=true; fi%0A+           if grep -Eq '^(scripts/validate-production-env\.mjs|lib/integrations/env|next\.config\.|vercel\.json|package-lock\.json)' <<< "$changed_files"; then environment=true; fi%0A+           if grep -Eq '^(app/|components/|public/|styles/|lib/i18n/|next\.config\.|postcss\.config\.|tailwind\.config\.)' <<< "$changed_files"; then ui=true; fi%0A+           if grep -Eq '^(app/.*/train/|components/train/|components/workouts/|lib/train/|services/activity-catalog/|app/api/activity-catalog/)' <<< "$changed_files"; then train_ui=true; fi%0A+%0A+           {%0A+             echo "code=${code}"%0A+             echo "runtime=${runtime}"%0A+             echo "dependencies=${dependencies}"%0A+             echo "database=${database}"%0A+             echo "integration=${integration}"%0A+             echo "scripts=${scripts}"%0A+             echo "environment=${environment}"%0A+             echo "ui=${ui}"%0A+             echo "train_ui=${train_ui}"%0A+           } >> "$GITHUB_OUTPUT"%0A+%0A+       - name: Set up Node.js%0A+         if: steps.scope.outputs.code == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         uses: actions/setup-node@v4%0A+         with:%0A+           node-version: 24%0A+           cache: npm%0A+%0A+       - name: Install dependencies%0A+         if: steps.scope.outputs.code == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         run: npm ci%0A+%0A+       - name: Lint changed source files%0A+         if: steps.scope.outputs.code == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             mapfile -d '' files < <(git ls-files -z | grep -zE '\.(ts|tsx|js|jsx|mjs|cjs)$' || true)%0A+           else%0A+             base_sha="${{ github.event.pull_request.base.sha }}"%0A+             mapfile -d '' files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" | grep -zE '\.(ts|tsx|js|jsx|mjs|cjs)$' || true)%0A+           fi%0A+           if [ "${#files[@]}" -gt 0 ]; then%0A+             npx eslint -- "${files[@]}"%0A+           fi%0A+%0A+       - name: Typecheck application%0A+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         run: npm run typecheck%0A+%0A+       - name: Run tests related to changed code%0A+         if: steps.scope.outputs.code == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then%0A+             npm run test:unit%0A+             exit 0%0A+           fi%0A+%0A+           base_sha="${{ github.event.pull_request.base.sha }}"%0A+           mapfile -d '' test_files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" | grep -zE '\.(test|spec)\.(ts|tsx|js|jsx)$' || true)%0A+           if [ "${#test_files[@]}" -gt 0 ]; then%0A+             npx vitest run --config vitest.unit.config.mjs "${test_files[@]}"%0A+           fi%0A+%0A+           mapfile -d '' source_files < <(git diff --diff-filter=ACMRT --name-only -z "${base_sha}...HEAD" \%0A+             | grep -zE '\.(ts|tsx|js|jsx)$' \%0A+             | grep -zEv '\.(test|spec)\.(ts|tsx|js|jsx)$' || true)%0A+           if [ "${#source_files[@]}" -gt 0 ]; then%0A+             npx vitest related "${source_files[@]}" --run --config vitest.unit.config.mjs --passWithNoTests%0A+           fi%0A+%0A+       - name: Check changed production dependencies%0A+         if: steps.scope.outputs.dependencies == 'true'%0A+         run: npm audit --omit=dev --audit-level=moderate%0A+%0A+       - name: Run changed script tests%0A+         if: steps.scope.outputs.scripts == 'true'%0A+         run: npm run test:scripts%0A+%0A+       - name: Run integration tests for API or database changes%0A+         if: steps.scope.outputs.integration == 'true'%0A+         run: npm run test:integration%0A+%0A+       - name: Set up Supabase CLI%0A+         if: steps.scope.outputs.database == 'true'%0A+         uses: supabase/setup-cli@v2%0A+         with:%0A+           version: 2.109.1%0A+           github-token: ${{ github.token }}%0A+%0A+       - name: Replay and lint database migrations%0A+         if: steps.scope.outputs.database == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           supabase db start%0A+           supabase db reset --local --no-seed%0A+           supabase db lint --local --schema public --level error --fail-on error%0A+           npm run migration:ledger:check%0A+%0A+       - name: Verify production database security contracts%0A+         if: steps.scope.outputs.database == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           database_url="postgresql://postgres:postgres@127.0.0.1:54322/postgres"%0A+           PGPASSWORD=postgres psql "$database_url" -X -v ON_ERROR_STOP=1 -f supabase/verification/train-atomic-rpc-security.sql%0A+           PLAIVRA_PREFLIGHT_TEST_DATABASE_URL="$database_url" node scripts/test-database-preflight-control.mjs%0A+           PGPASSWORD=postgres psql "$database_url" -X -v ON_ERROR_STOP=1 -f supabase/verification/production-release-migration-preflight.sql%0A+%0A+       - name: Validate production environment contract%0A+         if: steps.scope.outputs.environment == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           export PLAIVRA_VALIDATE_PRODUCTION_ENV=true%0A+           export PLAIVRA_RELEASE_ENVIRONMENT=production%0A+           export NEXT_PUBLIC_APP_URL=https://app.plaivra.com%0A+           export NEXT_PUBLIC_USE_MOCK_AUTH=false%0A+           export PLAIVRA_MCP_BASE_URL=https://app.plaivra.com/api/mcp%0A+           export PLAIVRA_OAUTH_ISSUER=https://app.plaivra.com%0A+           export PLAIVRA_CIMD_ALLOWED_ORIGINS=https://chatgpt.com%0A+           export CRON_SECRET=ci-placeholder-cron-secret-that-is-long-enough%0A+           npm run validate:env%0A+%0A+       - name: Build production application%0A+         if: steps.scope.outputs.runtime == 'true' || steps.scope.outputs.dependencies == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           export PLAIVRA_BUILD_TIMESTAMP="$(date -u +'%25Y-%25m-%25dT%25H:%25M:%25SZ')"%0A+           NEXT_PUBLIC_USE_MOCK_AUTH=false npm run build%0A+%0A+       - name: Install Playwright Chromium%0A+         if: steps.scope.outputs.ui == 'true'%0A+         run: npx playwright install --with-deps chromium%0A+%0A+       - name: Run relevant browser QA%0A+         if: steps.scope.outputs.ui == 'true'%0A+         shell: bash%0A+         run: |%0A+           set -euo pipefail%0A+           mkdir -p quality-reports%0A+           NEXT_PUBLIC_USE_MOCK_AUTH=true npm run dev > quality-reports/qa-server.log 2>&1 &%0A+           server_pid=$!%0A+           trap 'kill "$server_pid" 2>/dev/null || true' EXIT%0A+           for attempt in $(seq 1 60); do%0A+             if curl --fail --silent http://localhost:3000/ > /dev/null; then break; fi%0A+             sleep 1%0A+           done%0A+           curl --fail --silent http://localhost:3000/ > /dev/null%0A+%0A+           if [ "${{ steps.scope.outputs.train_ui }}" = "true" ]; then%0A+             QA_BASE_URL=http://localhost:3000 QA_TRAIN_EVIDENCE_DIR=quality-reports/train-qa-evidence npm run qa:train%0A+           else%0A+             QA_BASE_URL=http://localhost:3000 QA_EVIDENCE_DIR=quality-reports/rendered-qa-evidence npm run qa:rendered%0A+           fi%0A+%0A+       - name: Upload browser evidence on failure%0A+         if: failure() && steps.scope.outputs.ui == 'true'%0A+         uses: actions/upload-artifact@v4%0A+         with:%0A+           name: quality-failure-evidence-${{ github.run_id }}%0A+           path: quality-reports/%0A+           if-no-files-found: ignore%0A+           retention-days: 7%0A+%0A%0A ❯ lib/product/train-phase2a-architecture.test.ts:96:22%0A%0A
```

## branch-unit-failures.txt — final output
```text
FAIL  lib/product/muscle-intelligence-phase1-migration.test.ts > Muscle Intelligence Phase 1 migration contract > executes the disposable Phase 1 verification in the authoritative Quality database preflight
FAIL  lib/product/train-phase1-approved-ui.test.ts > approved Train Phase 1 UI contracts > keeps picker selection, duplicates, keyboard selection, focus return, request grouping, cancellation, and explicit pagination
FAIL  lib/product/train-phase1-approved-ui.test.ts > approved Train Phase 1 UI contracts > localizes detail, history filters, direct-session failures, and the active workout controller
FAIL  lib/product/train-phase2a-architecture.test.ts > Train Phase 2A architecture contract > enforces privacy, ownership, JSON shape, and verification in the authoritative quality gate
```

## main-unit-failures.txt — final output
```text
FAIL  lib/product/muscle-intelligence-phase1-migration.test.ts > Muscle Intelligence Phase 1 migration contract > executes the disposable Phase 1 verification in the authoritative Quality database preflight
FAIL  lib/product/train-phase1-approved-ui.test.ts > approved Train Phase 1 UI contracts > keeps picker selection, duplicates, keyboard selection, focus return, request grouping, cancellation, and explicit pagination
FAIL  lib/product/train-phase1-approved-ui.test.ts > approved Train Phase 1 UI contracts > localizes detail, history filters, direct-session failures, and the active workout controller
FAIL  lib/product/train-phase2a-architecture.test.ts > Train Phase 2A architecture contract > enforces privacy, ownership, JSON shape, and verification in the authoritative quality gate
```

## unit-baseline-diff.txt — final output
```text
```

