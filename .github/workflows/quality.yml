name: Quality

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: aw3b-one-shot-reconciliation-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  reconcile:
    if: github.event.pull_request.head.ref == 'feat/active-workout-aw3b-structured-set-details'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Apply deterministic reconciliation
        shell: bash
        run: |
          set -euo pipefail
          python scripts/aw3b-final-reconcile.py
          git show HEAD^:.github/workflows/quality.yml > .github/workflows/quality.yml
          rm scripts/aw3b-final-reconcile.py

      - name: Capture exact reconciliation validation
        shell: bash
        run: |
          set +e
          : > aw3b-reconciliation-validation.txt
          git diff --check >> aw3b-reconciliation-validation.txt 2>&1
          printf 'git_diff_check_exit=%s\n' "$?" >> aw3b-reconciliation-validation.txt
          node scripts/check-migration-ledger.mjs >> aw3b-reconciliation-validation.txt 2>&1
          printf 'ledger_check_exit=%s\n' "$?" >> aw3b-reconciliation-validation.txt
          cat aw3b-reconciliation-validation.txt
          exit 0

      - name: Commit reconciled diagnostic tree
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'docs(aw3b): reconcile production closure for validation'
          git push origin HEAD:feat/active-workout-aw3b-structured-set-details
