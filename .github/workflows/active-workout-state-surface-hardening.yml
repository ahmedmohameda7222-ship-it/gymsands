name: Active Workout State Surface Hardening

on:
  pull_request:
    branches: [main]
    types: [reopened]

permissions:
  contents: write
  pull-requests: read

jobs:
  harden:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'feat/active-workout-full-implementation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Apply bounded state-surface hardening
        run: node scripts/apply-active-workout-state-surface-hardening.mjs
      - name: Remove temporary hardening authority and commit
        shell: bash
        run: |
          set -euo pipefail
          git rm .github/workflows/active-workout-state-surface-hardening.yml scripts/apply-active-workout-state-surface-hardening.mjs
          git config user.name "plaivra-implementation"
          git config user.email "actions@users.noreply.github.com"
          git add --all
          git commit -m "Harden Active Workout paused and rest surfaces"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
