name: Active Workout Replacement Hydration Repair

on:
  pull_request:
    branches: [main]
    types: [reopened]

permissions:
  contents: write
  pull-requests: read

jobs:
  repair:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'feat/active-workout-full-implementation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Repair replacement hydration authority
        run: node scripts/repair-active-workout-replacement-hydration.mjs
      - name: Remove temporary repair authority and commit
        shell: bash
        run: |
          set -euo pipefail
          git rm .github/workflows/active-workout-replacement-hydration-repair.yml scripts/repair-active-workout-replacement-hydration.mjs
          git config user.name "plaivra-implementation"
          git config user.email "actions@users.noreply.github.com"
          git add --all
          git commit -m "Repair Active Workout replacement hydration authority"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
