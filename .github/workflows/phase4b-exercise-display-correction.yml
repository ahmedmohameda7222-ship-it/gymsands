name: Phase 4B Exercise Display Correction Helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: |
          python - <<'PY'
          from pathlib import Path
          exec(compile(Path("scripts/apply-phase4b-display-correction.py").read_text(), "scripts/apply-phase4b-display-correction.py", "exec"))
          PY
